@echo off
REM GROQ_API_KEY VPS .env me set ho chuki (gitignored). Secrets git me kabhi nahi.
REM Naya key daalna ho to SSH se /opt/leadgen/.env edit karo — yahan literal mat likho.
echo groq key already provisioned in /opt/leadgen/.env
