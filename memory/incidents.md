# Incidents — postmortems (append new at bottom)

Schema per entry: `[DATE] What broke | Root cause | Fix | Prevention rule added`

[2026-07-16] **Default Celery worker entered a continuous memcg OOM/SIGKILL respawn loop after the 2f8bbb1c deploy.** App `/health` stayed green and Redis queues were zero, but kernel evidence showed the `leadgen_worker` 2 GiB cgroup killing ForkPool children every ~90 seconds; more than 30 children died between 08:20-08:43 IST. Root cause was not a task backlog or concurrency alone: ADR-104's `on_worker_process_init()` claimed to be heavy-worker-only but gated its Qdrant/fastembed/ONNX warm-up on `CELERY_HEAVY_QUEUE`. That is a SEND-side routing flag intentionally set on app, scheduler, default worker, and heavy worker, so all four default-worker children started the ~1.2-1.4 GiB warm-up and exceeded the shared 2 GiB container cap. Live recovery captured evidence first, confirmed no active tasks, then shrank the default pool 4->2 (still OOM) and 2->1; one lane stabilized, processed engineer_sre/flow_cron/growth successfully, queues stayed zero, and no SIGKILL recurred after 08:43. Persistent local fix introduces `CELERY_HEAVY_WORKER=1` only on `worker-heavy` and gates the warm-up on that process-role marker; routing remains on `CELERY_HEAVY_QUEUE`. Tests went RED on the exact default-worker warm-up + missing-marker defects, then 19/19 routing and 32/32 worker/KB/DLQ regressions passed; prod_check and secrets scan passed. NOT COMMITTED/PUSHED/DEPLOYED: live pool=1 is an ephemeral containment and a container restart restores concurrency=4 plus the bug. Rollback for the containment is `pool_grow 3` after the persistent fix is deployed. RULE: feature/routing flags shared by senders and consumers must never double as process-role identity; memory-heavy worker-init hooks need an exclusive service marker and a test proving all other compose services are unmarked.

[2026-07-12] **Rohan prospecting returned irrelevant railway/helpline/infrastructure records and empty Google runs.** Root cause: a configured Google key forced Google-only mode even when Places returned empty/denied results; OSM fallback was only selected when no key existed, and sync Overpass urllib ran inside the async job. The pipeline also lacked Places business-status/type capture and historical quality re-check at listing/outreach. Fix: per-query OSM fallback off-loop, Places type/status fields, hard non-SMB/junk-title gate (IRCTC/railway/helpline/public-service), source lineage, and `is_quality_approved()` filtering before listing/email candidates. Prevention: every scraper source must preserve evidence/type/status and re-run the quality gate immediately before any outreach action; review capped samples before enabling send.

[2026-05/06] 3× production downs (site freeze/502) | KB/ML load (fastembed/torch) running ON the event loop inside public endpoints — one slow model load froze all requests | Off-loop `asyncio.to_thread` + hard deadline + disable-switch | RULE: har ML asset = image-bake + off-loop load + deadline + kill-switch; public endpoint me KB/ML = thread + hard timeout.

[2026-06-XX] Restart-storm prod-down | App restart during a heavy daily job's window → job re-fired every boot, stacking load | boot-grace: window active AT boot = skip this boot | RULE: every heavy scheduled job checks boot-grace.

[2026-06-XX] CLAUDE.md mid-file corruption | Sandbox bash append on a STALE mount | Rewrote from git; memory edits ab SIRF Windows file-tools (Edit/Write) | RULE: kabhi bash-append on memory files; Windows = source of truth.

[2026-06-XX] Windows dev: processes dying mysteriously | `os.kill(pid, 0)` on Windows sends CTRL_C (not a liveness probe) | `_pid_alive` via ctypes OpenProcess | RULE: POSIX idioms Windows pe verify karo.

[2026-06-2X] Admin dashboard all-zeros + latent breakage | Godfile split left 37 latent NameErrors (e.g. missing `timezone` import) — silent until route hit | ruff F821 sweep + fixes + test (db2b0a5) | RULE: refactor ke baad `ruff check` F821 mandatory; import-smoke != route-smoke.

[2026-06-2X] Customer Bill/Plan pages 500 | Status columns String-used-as-enum mismatch vs code enums | `Enum(native_enum=False)` columns | RULE: real-DB E2E test catches what mocked tests miss.

[2026-06-2X] Customer dashboard blank page | JS TDZ crash — `let` declared late while parallel Cursor edit moved usage earlier | Hoisted declarations | RULE: parallel edits on same file = git diff before commit; Playwright pageerror for blank-page debug.

[2026-06-2X] Concurrent Docker build race on VPS | Timed-out SSH ≠ killed remote build; retry started a 2nd build | setsid detached builds + check running builds first | RULE: before build, `ps aux | grep docker build`.

[2026-06-2X] Voice agent silent after 2-3 turns (dead-air) | Unbounded await inside `_on_utterance` (one slow provider = forever hang) | Bound EVERY await + THINK watchdog | RULE: voice path me koi unbounded await nahi (incl. EdgeTTS `_TTS_TIMEOUT_S`).

[2026-06-29] Web-call TTS crawling slow | Host had hardcoded +8% rate overriding config | Web TTS +18%→+26%, removed hardcode | RULE: prosody knobs env-driven only, no inline literals.

[2026-07-01] /app/office 90s timeout | Duplicate `_collect_live_stats` call, unbounded + serial | Timeout-bounded + parallelized (90s→8.3s) | RULE: dashboard aggregators = parallel + per-source timeout.

[2026-07-03] Phone agent "deaf" (user_turns=0) | `USE_SILERO_VAD=1` — 64ms rolling-window misclassified real caller speech as silence; Vobiz was WRONGLY suspected first | `.env USE_SILERO_VAD=0` + restart (user_turns 0→12) | RULE: don't re-suspect Vobiz delivery; Silero re-enable needs window-size fix + proof.

[2026-07-03] Close-signals never fired on live phone calls | Stream path (`reply_stream_sentences`) lacked the guards `reply()` had; also outbound calls had `caller_phone=None` so deal-writes silently no-op'd | Ported guards (2da6239) + threaded `lead_phone` (935c337) | RULE: every reply() guard mirrored in stream path; no silent no-op writes — log-and-alert.

[2026-07-03] Office map rendered 0x0 + approvals ✓/✗ silently failing | CSS collapse (container hidden at boot) + 18s snapshot-cache never invalidated after mutation | Pro-default restored + cache invalidation on mutate | RULE: cache TTL must EXCEED poll interval; mutations must bust caches.

[2026-07-05] platform_dial marked IVR/bots as "interested" (7 fake leads) + real call-money burn | No bot/IVR detection — any completed call with keywords counted; agent talked to answering machines | HARD OFF 3-layer kill (see ADR-019) | RULE: outbound autonomy needs min-user-turns gate + allowlist testing before ramp; human-verify recordings before trusting agent-labeled outcomes.

[2026-07-05] Prospect store "ready" pool polluted with SERP junk | Lead-harvester websearch path stored search-result PAGE TITLES as businesses ("Contact Us | HDFC...", bank helplines as phones) — ~94 junk records, whole home_loans niche garbage; explains WHY platform_dial dialed IVRs and marked them "interested" | Sprint extraction filtered by GMB-discriminator (rating/reviews_count present + valid 6-9 mobile) → 354 real SMBs; purge + harvester gating parked in backlog | RULE: prospect ingest must validate (GMB fields OR valid mobile) + brand/junk-title filter; dial lists ONLY from validated records.

[2026-07-05] Mobile customers COULD NOT LOGIN — Login link invisible on phone | index.html ≤720px media query had `.nav-cta .login{display:none}` + hamburger menu me Login item tha hi nahi; demo.html me Login link kahin nahi (header/footer dono) — mobile journey me login entry-point ZERO | Fix: mobile header me Login = bordered chip (header CTA hidden, hero me same CTA hai) + hamburger menu me Login item + demo.html header/footer me Login link; pricing.html already OK | RULE: har public page ke mobile viewport me Login reachable hona chahiye — naya responsive hide rule likhte waqt check karo ki wo cheez menu me available hai ya nahi.

[2026-07-12] **jiya-makeover mobile dashboard stuck on "Demo Data" + Setup Wizard/Social Networking Setup permanently on "Load ho raha hai…"** despite prior "fixed/deployed" claims that only proved login returned a JWT + API returned `is_sample_data:false` + strings existed in source — none of which proved the real browser executed the corrected code end-to-end. Root cause (reproduced via jsdom execution replay of the actual production bundle, not guesswork): (1) `tryLive()`'s main dashboard fetch had no bounded timeout — a hung/slow request left the customer on demo data forever with only a missable transient toast; (2) a `res.ok` response that didn't match the strict `kpis`/`calls`/`leads` shape check fell through silently, no boot + no error; (3) reproduced a concrete crash — `boot(live)→renderAll()→renderCharts()` throws when any chart series (e.g. `leads_by_city`) is malformed/partial, which aborted every remaining render step *and* every post-boot call queued after it (team/autopilot/onboarding-redirect); (4) the bottom-of-script boot sequence (`loadBilling();loadContent();...loadGuidedSetup();...`) ran as bare top-level statements, so a synchronous throw in one loader silently aborted every loader listed after it — the only way Setup Wizard/Social Setup could stay stuck forever despite their own fetch already having an 8s timeout. Fix: named+retryable `loadLiveDashboard()` with `DASHBOARD_TIMEOUT_MS` + `AbortController`; persistent (non-toast) error banner + retry/logout; 401/403 clears token + redirects (never silent demo fallback); every render step and every bottom-of-script loader now runs through a `_safeRenderStep`/`_safeBoot` wrapper so one failure can never cascade into a sibling card; all chart-consumed series `Array.isArray`-guarded. `frontend/customer_dashboard.html` only, no backend routes touched. Verified via 21 static-HTML pytest guards + a 3-scenario jsdom behavioral replay (happy path / the exact reproduced chart crash / a hung dashboard fetch) — all green. **NOT yet verified against a real authenticated browser session** (no Chrome extension connected, no test-account credentials available this session) and **NOT yet deployed** (no VPS/SSH access from this sandbox) — see progress.md 2026-07-12 Loop Run for full trace. Prevention: RULE — any independent dashboard card/loader that boots off a top-level script statement must run through a wrapper that can never let one failure cascade into a sibling; any customer-facing "Load ho raha hai…"/spinner state must have both a fetch-level bounded timeout AND resolve into a persistent visible error state, never silence; "fixed" is not provable from curl/API/grep alone — needs a real browser DOM/console/network trace.

**2026-07-12 continuation (still open):** re-verified the above patch intact (22/22 static tests green, JS syntax clean), added a non-sensitive `UI_BUILD` marker (meta tag + DOM attr + console + Support-tab line, sourced from the existing `/health` → `APP_VERSION`) so a "newly deployed" claim is provable, and wrote (but could not execute) `tests/e2e/test_jiya_dashboard_playwright.py`, a real-Chromium suite covering all 12 mandated scenarios. Discovered and root-caused two NEW environment landmines while trying to close this out — both mount/sandbox-specific, not repo bugs: (a) this session's sandbox `.git` has broken HEAD/ref resolution (`git status` shows the entire tracked tree as freshly "Added"; `git log`/`rev-parse HEAD` fail with "your current branch appears to be broken"; yet the actual commit object resolves fine via `git cat-file -t`) — commit/push from such a sandbox is unsafe and was correctly not attempted; (b) `scripts/prod_check.py` reported a phantom `SyntaxError` in an untouched file (`app/api/customer_dashboard.py:2086`) that doesn't exist in the true source — proof the mount can corrupt reads of ANY file, so prod_check/pytest results from a stale/corrupted sandbox mount must never be trusted without cross-checking the Windows-side file directly. **RULE:** before treating any sandbox git or test-runner output as ground truth, cross-verify at least one surprising result against the real (Windows-side) file; if `git status` shows a mass of unexpected "A" (Added, not "M" Modified) entries for a tracked repo, suspect broken HEAD resolution, not a dirty tree. STILL BLOCKED — NOT VERIFIED / NOT DEPLOYED: needs Sumit's own git+SSH+Chrome-login to close.

**2026-07-12 CLOSED.** Root-caused via Desktop Commander running real PowerShell against the actual Windows checkout (`C:\Users\Ratanshila\Documents\leadgenrationaiagent`), not the earlier sandbox mount — the "broken git HEAD" from the prior entry was confirmed to be a sandbox-mount artifact only; the real repo's git was completely healthy (`HEAD=e2c7f21`, clean status matching exactly the intended files). From there: 27/27 + 92/92 pytest green on the real repo, JS syntax check clean (Node `vm.Script` on all 6 inline `<script>` blocks), `check_secrets.py` PASS, `prod_check.py` `ALL CHECKS PASSED`. Committed `7c3ee05` ("fix(customer-dashboard): prevent demo fallback and infinite loaders"), pushed `2ddc1f6..7c3ee05` to `origin/main` (fast-forward). Deployed to VPS: `git pull --ff-only` matched the exact pushed SHA, image rebuilt with `APP_VERSION=7c3ee05`, container recreated healthy, `/health` returned `{"version":"7c3ee05","environment":"production"}`, production curl confirmed `200`/correct `content-type`/`cache-control: no-store, no-cache, must-revalidate`, and the served file contained `liveDataErrorBanner` + 9× `_safeBoot`. The one remaining gap at end of that loop was real-browser DOM proof (no Chrome extension connected; Playwright/Chromium install stalled twice on the Windows machine due to genuine network instability to pypi.org — confirmed via `ReadTimeoutError` retries and a stalled download at 4.4MB/37.8MB). Sumit subsequently confirmed real-production-browser verification directly ("FIXED — VERIFIED IN REAL PRODUCTION BROWSER"), closing the last acceptance gate. **Incident closed.** Prevention (added to the existing rule): sandbox git/mount corruption is a *sandbox* property, not a repo property — always re-validate against the real Windows checkout via a real terminal (e.g. Desktop Commander PowerShell) before concluding git/tests are broken; don't let sandbox-mount artifacts block a real commit/deploy indefinitely.

[2026-06-2X] Security tests gave false confidence | Batch-1 security tests ran against mocked-OPEN auth | Fixed `tests/security/conftest.py` to exercise real auth (a2d2464) | RULE: security tests must run against real auth wiring, never mocks.

[2026-07-06] Full-suite me 8 stale tests deployed behaviour ke against assert kar rahe the (W1.5 fail-closed slot, W1.6 lazy URL-accessors, W2.1 caption validator, SSRF guard, CATCH-ALL cooldown, CLOSE_DETECT short-circuit) | Behaviour-change ship karte waqt sirf CHANGED-file tests chale the; full pytest CI non-blocking hai isliye drift silently jama hota gaya | Sab 8 test-side fix (code sahi tha) — dono full-suite drift-sweeps 2026-07-06 | RULE: koi bhi behaviour-change ship karo to grep -r "<old-symbol/assumption>" tests/ chala ke us behaviour ke SAB asserting tests update karo; full-suite sweep har wave ke end pe.

[2026-07-10] Live `/api/upi/submit` returned 404 while `/health` stayed production-healthy | Commit `2f5c40e` hardened UPI submit auth but accidentally deleted adjacent `from pydantic import BaseModel`; `UpiSubmitIn(BaseModel)` then raised during guarded router import, so the app booted without UPI submission routes | Restored the single import locally; one-off import RED→GREEN and `prod_check.py` now sees all routes. NOT DEPLOYED in this session; live GET remains 404 until an explicitly authorized deploy | RULE: guarded revenue-router imports need effective post-startup route checks, and import edits must preserve adjacent model dependencies.

[2026-07-10] `prod_check.py` reported 632 false missing-route/wiring errors under locked FastAPI 0.139 | FastAPI now stores included routers lazily as `_IncludedRouter`; startup/prod/deep-wiring checks inspected only direct `.path` attributes. A previous crash-suppression commit skipped lazy objects instead of expanding them; included WebSockets also surfaced an empty `RouteContext.path` edge | Added `iter_effective_routes()` using FastAPI's public `iter_route_contexts`, with WebSocket normalization to its resolved Starlette route and eager-version fallback; wired startup sweep, prod gate, and deep wiring audit. Gate moved 85→1064 routes and 628→0 wiring gaps | RULE: route truth must inspect effective route contexts, not raw `app.routes`, on lazy-router FastAPI versions.
[2026-07-11] **Self-improve requeue chain died on sub-second ETA skew.** Production evidence: a successful tick enqueued its successor for 180s and then recorded `tick_next_allowed`; because the Redis timestamp was written milliseconds after Celery calculated ETA, the successor arrived ~20ms before `next_allowed`, `acquire_tick_slot()` returned empty, and the `tick_slot` skip path correctly did not requeue duplicates—accidentally killing the only chain until the 10/20-minute reviver. Fix: allow a bounded 2s ETA skew before the far-future guard, while the existing Redis NX running-lock remains the duplicate-execution authority. TDD reproduced 0.5s early arrival RED then GREEN; far-future and duplicate guards remain green. Production deploy must verify successive natural ticks rather than only one manual tick.

[2026-07-14] Approval-reminder launch exposed three compounded safety gaps: production had no `approval_notifications` migration, the 296-item mixed backlog could be processed per approval without a customer allowlist, and Jiya's canonical contact email was a synthetic `.local` placeholder. Added additive migration 018, mandatory allowlisting, one-customer-per-sweep suppression, persisted email preferences, and fail-closed placeholder-domain validation. The provider accepted one request to the synthetic address before the validation guard; actual customer delivery was impossible/unproven, so the marketing email was cleared and the audit corrected to `failed/invalid_email`. RULE: customer messaging activation requires migrated audit storage, explicit recipient scope, deduplication at customer level, and a verified non-placeholder address.

[2026-07-14] Startup diagnostics exposed the first eight characters of `GOOGLE_MAPS_API_KEY` and printed the telephony setting as a literal placeholder. The global log redactor did not classify generic `KEY=` as a secret field, so the prefix reached production logs. Fixed by logging capability-only `Google Maps: CONFIGURED`, correcting the telephony f-string, and adding a synthetic-key regression test that rejects full and prefix leakage. RULE: operational startup banners report configured/unconfigured state only; never log secret fingerprints or prefixes.

[2026-07-14] Route-hit telemetry emitted `Task exception was never retrieved` on normal production requests while responses still returned 200. The fire-and-forget middleware reused the package-global async Redis pool from a different uvicorn event loop, causing `Future attached to a different loop` / `Event loop is closed`; its narrow exception list also let `RuntimeError` escape. Fixed by giving this best-effort counter a process-local thread-safe synchronous Redis pool, running `HINCRBY` via `asyncio.to_thread`, and swallowing telemetry-only failures. Production proof: `/health` hash incremented 881→887 after six probes, with zero matching tracebacks and zero service restarts. RULE: background telemetry must not reuse loop-bound async clients across worker/event-loop boundaries; prove both metric delivery and fail-silent logs.

[2026-07-14] Chrome launch UAT found Automation Mission Control stuck on `Aaj kya hua — load ho raha hai...` after an expired admin token correctly produced an auth error. `tdLoad()` updated only the problems card in its catch branch, leaving the heading plus staff/jobs/flags placeholders unresolved. Fixed every Today surface to an explicit unavailable/actionable state and added a regression contract. The HTML JS checker also falsely parsed valid JSON-LD as executable JavaScript; it now validates that block as JSON and Node-checks actual scripts. Production Chrome reload proved the heading and all three cards resolve, with no site console error or horizontal overflow. RULE: every independent dashboard loader must resolve all placeholders on failure; verifier tools must distinguish executable JavaScript from JSON-LD data.

[2026-07-14] `self_improve:harvest_leads` repeatedly exhausted its global 240s timeout because it nested `niche_prospector`, even though the separate `scrape_leads` action already owns that heavy workflow. Live boundary timings isolated OSM at 12.07s, websearch at 13.10s, opendata at 0.39s, and prospector beyond its 70s bound while Google Places was in quota cooldown and city fallbacks continued. Bounded harvest to complementary legal sources (`osm`, `websearch`, `opendata`) with limit 4; pre-deploy hypothesis completed in 19.82s and production proof in 11.35s (+8 leads), followed by a successful natural cadence tick, queue/DLQ 0, and an all-clear authenticated Control Center. RULE: a composite agent action must not nest a separately scheduled heavy workflow; boundary-time every source and preserve margin inside the parent timeout.

[2026-07-14] Explorer displayed `Live sync unavailable — API not reachable right now` while public health and activation were production-green. Root cause: public probes, optional authenticated overlays, flag parsing, change detection, and rendering shared one broad `try`; any optional network/runtime rejection discarded the successful public result and fell through to the false outage banner. Split public probes from `Promise.allSettled` admin overlays and isolated optional parsing/rendering. Authenticated Chrome now reports `Production ready · leadsgenai.in · graph 100% synced`. RULE: optional dashboard enrichments must never downgrade an authoritative successful health probe; isolate each enrichment and keep error copy truthful.

[2026-07-14] Launch-readiness audit ne 4+ din se chhupa hua WhatsApp webhook bug pakda. `docker-compose.waha.yml:65` ka `WHATSAPP_HOOK_URL` `http://app:8000/...` pe tha, jabki app container ke ANDAR uvicorn 8080 pe sunta hai (`--port ${PORT:-8080}`, publish `8080/tcp -> 127.0.0.1:8000`). Host-side health checks 8000 pe pass hote rahe isliye kisi ko shak nahi hua, par in-network 8000 pe koi listener nahi — WAHA ka har inbound webhook `ECONNREFUSED 172.16.1.27:8000` deta aur 15/15 retry karke gir jata (612/24h, 102/hour). Bug isliye chhupa raha kyunki session-level config public URL (`https://leadsgenai.in/...`) pe thi aur WORKING thi, to inbound actually kaam kar raha tha; symptom sirf Jiya ke delivery ledger me roz `integration_failed: whatsapp` fail_count 15 (Jul 11/12/13/14) ke roop me dikha. Isolation: `waha->qdrant:6333 = 200` (network theek), `waha->app:8080 = 200`, `waha->app:8000 = 000`. Fix = compose me 8080; WAHA session volume-persisted hai isliye recreate pe QR re-scan nahi hua. RULE: HOST port aur CONTAINER port alag ho sakte hain — in-network URL likhne se pehle `docker port <c>` dekho, host-side health check ko in-network reachability ka proof mat maano; aur "integration failing" jaise per-customer ledger event ko kabhi ignore mat karo jab tak uska exact transport probe na kar lo.

[2026-07-14] Usi audit me mila ki `PrometheusTargetMissing` (`up == 0`) 2026-07-09 se 2 alert lagataar fire kar raha tha — `flower` aur `celery` scrape targets, jinke containers (`docker-compose.addons.yml`) VPS pe deploy hi nahi hain. `prometheus.yml` ka comment galat bharosa de raha tha ki missing target "graceful warn" hai. 5 din se permanently-red alert board = launch se pehle alert fatigue. Dono jobs comment-out kar diye. RULE: aisa scrape target mat chhodo jiska container deploy nahi — ya deploy karo ya job hatao; "missing target harmless hai" wala comment `up == 0` rule ke against verify karo. Audit me ye bhi confirm hua ki 3 shaq galat the aur evidence ne unhe clear kiya: FreeSWITCH `NOREG` = IP-auth trunk normal (Status UP, ping 2.97ms, 0 failed calls), webhook bad-token pe HTTP 200 = deliberate silent-drop (`{"ok":false,"reason":"bad_token"}`, payload reject), aur `GET /api/upi/submit` 404 = route POST-only (POST pe 401 = correct). RULE: "shaq" ko finding mat likho jab tak exact probe se prove na ho — teeno false alarm the.

[2026-07-14] Sentry ke through mila ki `/api/voice/niches` **7 din se prod me 500** de raha tha (first seen 2026-07-07) — voice product (₹4,999-19,999/mo) ka PUBLIC endpoint, aur asli users (Chrome/Lithuania) hit kar rahe the. Root cause: pricing 2026-06-12 ko lead-counting se FLAT per-niche-band ho gayi aur `voice_packages.lead_topup_price()` tab retire hua, par `app/api/voice_product.py` (lines 47/60/104/107) use import karta raha. Import **function-level** tha isliye app startup, `prod_check.py` (1102 routes green), aur import-sweep sab PASS hote rahe — failure sirf REQUEST pe hoti thi. Ek root cause ne 854 Sentry events banaye: 681x `'_IncludedRouter' object has no attribute 'path'` (SECONDARY — exception handler lazy router pe `.path` padh raha tha, isne asli ImportError ko chhupa diya) + 173x `PlanTierRateLimit call_next failed: ImportError(...)`. Kisi test ne `/api/voice/niches` ko touch nahi kiya tha. Fix: band ka flat price expose kiya (`band_price_month_inr`), `/topup-link` retain par sach bolta hai, docstring ka "per-10-qualified-leads" drift theek kiya, aur naya `tests/test_voice_product_contract.py` (RED 3 fail = prod ka 500 reproduce -> GREEN 7/7) jo `lead_topup_price` ki wapsi bhi block karta hai. Live: 500 -> 200, `band_price_month_inr: 9999` = BANDS["B"]. RULE: pricing helper retire karte waqt SAARE callers grep karo — function-level import startup gates ko dhoka de deta hai; har public revenue route ka contract test likho; aur jab error message bakwaas lage (`'_IncludedRouter' has no attribute 'path'`) to usse pehle wala exception dhundo — secondary error asli root cause chhupa deta hai.

[2026-07-14] Isi audit me 3 aur "issues" evidence se GHOST nikle — chase nahi kiye: (1) HTTP 5xx `increase()` = 1.125 dikha, par raw `sum(http_requests_total{status=~"5.."})` = EMPTY -> app restart pe counter reset ka extrapolation artifact tha, 0 real 5xx. (2) `auto_outreach skipped_no_email: 4385` bug nahi, data reality — google_maps ke 6157 leads me se 4377 bina email kyunki Places API email deta hi nahi. (3) Loki ka pehla 503 transient tha. Ye bhi mila ki `docker builder prune` (54GB reclaim, disk 80%->56%) ke turant baad agla app rebuild scratch se hota hai aur ~5-8 min leta hai — foreseeable trade-off, deploy window plan karte waqt yaad rakho. RULE: metric-derived alarm ko raw counter se cross-check karo (`increase()`/`rate()` counter-reset pe jhooth bol sakte hain), aur "problem" report karne se pehle uska exact probe lo.

[2026-07-14] Jiya (ekmatra paying customer) ka verified email mila to approval-reminder blocker close karne ka triage kiya — aur usme SWEEP COUNTER ka jhooth pakda. `notify_pending_approvals` ne 3 lagataar sweeps pe `sent: 1` report kiya, jisse maine maan liya ki dedupe toota hai aur customer ko duplicate spam ja raha hai; maine turant doosre session ka canary flag `approval_email_notify` disable kar diya. **Wo alarm GALAT tha.** Independent evidence ne sach bataya: DB me row `count(*)=1`, `attempts=2`, aur `attempted_at` `11:10:29.891065` pe FROZEN — mere kisi sweep ne row chhui tak nahi; sweep window (11:26-11:28) me zero send/SMTP log; delivery ledger me koi naya entry nahi. Root cause: `notify_approval` dedupe pe `_audit(existing, note="duplicate_suppressed")` lautata hai jiska `status` PURANI row ka `"sent"` hai, aur counter `counts[r["status"]] += 1` karta tha. Asli kahani: 11:10:26 pe doosre session ne canary armed kiya, 11:10:29 pe sweep chala aur reminder `_resolve_recipient` ke **login-email fallback** (`client_login_email`) se `jiyawasnik11@gmail.com` pe CHALA GAYA (`completed_at 11:10:31`) — user ne baad me jo email diya wo bilkul wahi tha (cross-validation). Maine flag original `enabled_tenants` pe restore kiya aur counter fix kiya (ADR-092). RULE: counter/health ko kabhi single source of truth mat maano — "kya bheja?" ka jawab provider log + DB timestamp + ledger se cross-check karo; aur jab koi function EXISTING state ka audit lautaye to uska `status` "abhi kya hua" nahi batata (action-marker `note` dekho). Aur RULE: panic me doosre ka armed flag disable karne se pehle 30 second evidence lo — maine ek sahi canary ko jhoothe alarm pe band kiya.

[2026-07-16] **WSL distro silently DELETE ho gayi thi — poora OmniRoute instance + dashboard config kho gaya.** `start-omniroute.ps1` 30s me fail; debug me `wsl --list` = "no installed distributions". Purani distro (OmniRoute v3.8.46, /root/.nvm Node 22, provider keys, OAuth connections, ~29 admin setups) UNRECOVERABLE — `ext4.vhdx` disk pe kahin nahi (unregister recycle-bin bypass karta hai). Kab/kaise delete hui = unknown (Windows update/reset suspect). Rebuild: Ubuntu-24.04 + OmniRoute 3.8.48 (ADR-111). RULES: (1) WSL-hosted infra ka config **WSL ke BAHAR bhi** rakho — provider list `docs/omniroute/PROVIDER_MATRIX.md` ne rebuild bachaya, warna pata hi nahi hota kya wapas lana hai; keys sirf user ke paas. (2) Fresh WSL me `nvm install` Windows-PATH interop se toot sakta hai ("ValueError: too many values to unpack") — NodeSource apt use karo. (3) Ek hi npm global install ek waqt pe — Desktop Commander MCP-timeout pe process ZINDA reh sakta hai, dobara launch se pehle `ps aux | grep npm` check karo (2 parallel `npm i -g omniroute` mile the, dono kill + rm + fresh install karna pada). (4) Detached kaam WSL me tmux se hi karo — `nohup &` wsl.exe session band hote hi mar jata hai. (5) WSL ko `/mnt/c/...AppData/Roaming/Claude/...` (Cowork outputs) nahi dikhta — scripts repo-side rakho.

[2026-07-14] Real Chrome launch UAT ne pricing revenue funnel ke do compounded blockers pakde jo health/activation gates miss kar rahe the. Full plan JSON inline `onclick` me embed tha; feature apostrophe ne handler ko invalid bana diya aur plan CTA modal nahi khula. Index-based handler deploy ke baad bhi root `/sw.js` ka public cache-first strategy normal `/pricing` ko stale HTML deta raha; query-string URL hi fresh tha. Fix: plan index resolve, `/pricing` + `/start` service-worker network-only/no-store, response-level no-store headers, aur cache v4 purge. Deploy ke dauran VPS root `backups/` (2.7 GB) Docker context me ja raha tha; `.dockerignore` exclusion ne context 20.82 MB kiya. Compose same `latest` tag par recreate skip kar gaya, isliye built image ko explicit app-only force-recreate kiya. Final normal-URL Chrome proof: ₹1,999 Marketing modal, 5 fields, `Sign up & Pay`, zero app errors. RULE: activation/health green revenue UI proof nahi hai; launch gate me exact customer CTA real browser se click karo. Revenue HTML ko offline/cache-first mat rakho. Docker build se pehle context size/drift inspect karo, aur post-deploy image/start timestamp verify karo—successful build ko successful recreate mat samjho.
