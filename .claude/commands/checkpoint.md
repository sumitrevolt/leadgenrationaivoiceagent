---
description: Progress checkpoint banao — git commit + 1-2 line milestone docs/SESSION_LOG.md me.
---
# /checkpoint — progress + memory checkpoint

Memory discipline: **CLAUDE.md = LEAN** (har turn load hoti — sirf 1-2 line update) · **`docs/SESSION_LOG.md` = detailed dated history** (auto-load NAHI hoti — yahan append karo).

## Create
1. **`/verify quick`** — state clean ho.
2. **Git commit** current work (Windows git, simple message).
3. **`docs/SESSION_LOG.md` me append** — date + 1-2 line "kya bana/fix hua + commit SHA + key gotcha". CLAUDE.md me sirf 1-2 line (token discipline; rebuild-mat-karo notes).
4. Report: SHA + log line + route count (agar badla).

## List
`docs/SESSION_LOG.md` ke recent milestones + `git log --oneline -10`.

`$ARGUMENTS`: `<milestone name>` (optional).
