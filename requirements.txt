# AI Voice Agent - B2B Lead Generation Platform
# Production Dependencies

# =============================================================================
# Core Framework
# =============================================================================
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.3.0
python-multipart==0.0.7

# =============================================================================
# Database
# =============================================================================
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.20.0
alembic==1.13.1
redis==5.0.1
aioredis==2.0.1

# =============================================================================
# AI/ML - Cloud-based APIs
# =============================================================================
openai==1.12.0  # also drives Groq/Cerebras/OpenRouter free LLMs (OpenAI-compatible base_url) — see app/voice_agent/free_ai.py
anthropic==0.18.0
# langchain==0.1.6        # unused in codebase; pin conflicts with langchain-core>=0.3 (langgraph)
# langchain-openai==0.0.5 # unused in codebase; same conflict
google-generativeai==0.3.2
google-genai>=1.0  # Gemini audio-in STT (phone hearing) — NEW SDK, namespace google.genai (old SDK ke saath coexist karta hai)
google-cloud-aiplatform==1.42.1

# =============================================================================
# Vector RAG (Qdrant + fastembed e5 embeddings — KB backend)
# =============================================================================
qdrant-client>=1.12.0
fastembed>=0.4.0

# =============================================================================
# Speech Processing
# =============================================================================
# STT
deepgram-sdk==3.0.0
google-cloud-speech==2.24.0
# Local STT for the conversational phone pipeline (app/voice_agent/phone_stream.py
# + app/telephony/vobiz_stream.py) — faster-whisper 'tiny' runs CPU/int8, no API key.
faster-whisper>=1.0.0
# audioop-lts>=0.2.1   # ONLY on Python 3.13+ (audioop stdlib se hata diya gaya); 3.12 me built-in hai

# TTS
elevenlabs==1.0.3
azure-cognitiveservices-speech==1.35.0
edge-tts>=7.2.0  # 6.1.9 gets HTTP 403 from MS endpoint (Sec-MS-GEC token) — must be 7.x

# =============================================================================
# Telephony
# =============================================================================
twilio==8.10.0

# =============================================================================
# Payment Gateways
# =============================================================================
stripe>=8.0.0
razorpay>=1.4.0

# =============================================================================
# Web Scraping
# =============================================================================
playwright==1.41.0
beautifulsoup4==4.12.3
selenium==4.17.0
googlemaps==4.10.0

# =============================================================================
# HTTP Clients & Email
# =============================================================================
requests==2.31.0
aiohttp==3.9.3
httpx==0.26.0
aiosmtplib==3.0.1

# =============================================================================
# CRM Integrations
# =============================================================================
hubspot-api-client==8.2.1
google-api-python-client==2.115.0
google-auth-oauthlib==1.2.0
gspread==6.0.0

# =============================================================================
# Utilities
# =============================================================================
python-dotenv==1.0.1
pydub==0.25.1
phonenumbers==8.13.28
APScheduler==3.10.4
Pillow==10.2.0

# =============================================================================
# Background Tasks
# =============================================================================
celery==5.3.6

# =============================================================================
# Logging & Monitoring
# =============================================================================
loguru==0.7.2
sentry-sdk==1.39.2
psutil>=5.9.0

# =============================================================================
# GCP Production
# =============================================================================
google-cloud-secret-manager>=2.18.0
google-cloud-storage>=2.14.0
google-cloud-logging>=3.9.0
google-cloud-monitoring>=2.18.0
google-auth>=2.27.0

# =============================================================================
# Authentication
# =============================================================================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# =============================================================================
# Data Processing
# =============================================================================
pandas==2.2.0
numpy==1.26.3
scikit-learn>=1.3.2

# =============================================================================
# Agent Orchestration (LangGraph supervisor — app/agents/)
# =============================================================================
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3
fastapi-mcp>=0.3  # platform endpoints as MCP tools (/mcp)

# =============================================================================
# Voice Pipeline (P3 — FreeSWITCH + Pipecat, optional/heavy)
# =============================================================================
# pipecat-ai>=0.0.60  # P3 voice pipeline — install when wiring audio (heavy)

# Vobiz WebSocket streaming STT (app/telephony/vobiz_stream.py) — pick ONE.
# Both heavy + optional; the WS degrades gracefully (deaf) if neither present.
# vosk>=0.3.45            # offline STT; set VOSK_MODEL_PATH to a model dir
# faster-whisper>=1.0.0   # alt offline STT ("tiny" model, CPU int8)
# TTS uses edge-tts (above) + pydub (below); both need ffmpeg on the host.
# NOTE: ffmpeg is already installed on the VPS (deploy_vps.sh STEP 1) —
#       pydub shells out to it to decode EdgeTTS MP3 -> PCM for µ-law.

# =============================================================================
# Testing (dev only - move to dev requirements for production)
# =============================================================================
pytest==7.4.4
pytest-asyncio==0.23.4
pytest-cov==4.1.0