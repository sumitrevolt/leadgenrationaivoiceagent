# Installed at image-build time from the committed lockfile — never committed.
node_modules/

# Render scratch space. Real outputs go to the canonical media roots
# (app.marketing.video_media_paths), never here.
renders/
.debug/

# Demo render artifacts generated by local runs — never committed.
# Canonical media lives in app.marketing.video_media_paths, not here.
demo_leadgen.mp4
demo_leadgen_manifest.json
frame_*.png
