# progress.md — Loop Engineer Ledger (LeadGenAI)

<<<<<<< HEAD
## Loop Run
Date: 2026-07-15
Goal: Enterprise launch-readiness loop (master prompt) — real baseline, prod route smoke, follow-up-audit reconciliation, fix the remaining verified gap (reply-agent gambling-spam classification), evidence-backed launch verdict.
Inspected: CLAUDE.md + memory/INDEX.md + progress.md (top loops) · live prod `/health`+`/health/ready` (cache-busted) · `/api/billing/plans` · `/api/voice/niches` · `/api/public/pay-info` · `/app/login` · `/start` · git ancestry (prod 5f65979c vs local 0350ee18 vs origin f6fb352a) · `app/platform/reply_agent.py` full guard family · sandbox-vs-Windows file truth for voice-KB files + admin hardening files.
Problems Found: (1) DEPLOY GAP — 10 committed+pushed commits NOT on prod (admin confirmations, password-reset/onboard-scrape hardening, L2 fix, Postiz readiness); local 1 behind origin (ff-only). (2) Reply agent had NO content-level spam guard — betting spam ("Reddy Anna") classified `interested`, draft in Hot Queue (07-14 audit item, unfixed till now). (3) Sandbox mount served phantom staged-revert (10 files/-735 lines incl. staged-DELETE of 4 ADR-104 test files) — Windows disk verified INTACT; operator must confirm real `git status` on Windows before any commit. (4) Fetch-proxy served month-old poisoned `/health/ready` (`version:"latest"`) — ADR-100 residual confirmed live.
Changed: `app/platform/reply_agent.py` (+`_SPAM_CONTENT_RE`/`_is_spam_content()`, wired at email loop pre-classify + `whatsapp_reply()` entry + `_is_noise_row()` read-path retro-hide; flags `REPLY_SPAM_CONTENT_GUARD` default-ON, `REPLY_SPAM_EXTRA_TERMS` CSV) · NEW `tests/test_reply_agent_spam_guard.py` (19 cases incl. near-miss legit "booking id") · `memory/decisions.md` ADR-105 · this ledger entry. No commit/push/deploy (user gate §8).
Tests Run: sandbox pytest unavailable (pip flaky, known) — deterministic harness: HEAD blob + the exact 4 edits re-applied programmatically (each anchor count==1 asserted), AST OK, real test file executed via pytest-stub → **19 passed, 0 failed**. Live smoke: `/health` 200 v5f65979c production · `/health/ready?cb=` all checks healthy · billing plans = 2 public (Growth hidden ✓) · voice niches 200/28 · pay-info UPI ARMED · login + /start render correct.
Verification Evidence: prod version == deployed SHA (no `:latest`); MRR payment-evidence fix c78b73d PROVEN in prod lineage (merge-base); voice-KB fix 8383eec PROVEN in prod lineage; Windows files intact where mount claimed reverts (grep counts vs HEAD blob match).
Risks: Windows venv pytest for the new suite not run this session (sandbox-only proof — operator should run `pytest tests/test_reply_agent_spam_guard.py -q` + `prod_check.py` before commit). Spam guard is default-ON noise-guard (not a compliance gate); rollback = `REPLY_SPAM_CONTENT_GUARD=0` env, no deploy needed after flag set.
Remaining: (1) USER: deploy the 10-commit backlog (`git pull --ff-only` then standard `deploy_vps.sh` with `APP_VERSION=f6fb352a`); (2) USER: verify real `git status` on Windows — if staged deletions of ADR-104 tests actually appear, `git restore --staged .`; (3) own-brand posting end-to-end proof still pending (needs VPS `data/social_post_jobs.jsonl` non-empty post_id); (4) Postiz open-registration + YouTube OAuth publish (already in Current State); (5) email warmup paused / approval backlog — operational.
Next Highest Priority: user-run deploy of the pushed backlog, phir live acceptance (health version == f6fb352a + admin confirm-modals smoke).

## Loop Run
Date: 2026-07-15
Goal: ADR-104 Voice QA incident — implement Phase A4.4 (`_kb_facts()` rewrite, remove inline catalog bootstrap from the live voice reply path), A4.5 (owned single-niche refresh Celery task with Redis dedup lease), A4.6 (fix ~185x duplicate vector writes in `load_niche_faqs`). SCOPE per explicit user decision (AskUserQuestion): implement + test only, no commit/push/deploy — user reviews and runs those themselves from a prepared runbook.
Inspected: `memory/decisions.md` ADR-104 addenda #1-#7 (full incident chain, measured root causes) · `app/voice_agent/telecaller_brain.py` (3126 lines, full read) · `app/voice_agent/kb_readiness.py`/`knowledge_base.py`/`kb_loader.py` · `app/platform/kb_refresh.py` (confirmed distinct domain — customer-website re-ingest, not niche-catalog) · `app/agents/self_improve.py` (Redis lease pattern precedent) · `app/worker.py` task-registration conventions · `tests/test_kb_loader_scoped.py`/`test_kb_readiness.py`/`test_kb_point_id.py`/`test_kb_delete_before_reseed.py`/`test_celery_queue_routing.py`.
Problems Found: none new — implemented against the already-measured ADR-104 root causes (inline 39-niche bootstrap on the voice hot path; abandoned-future thread-leak on `asyncio.wait_for` timeout; `load_niche_faqs` never invoking the pre-existing `replace_source=True` dedup mechanism). One NEW sandbox-only finding: this Cowork bash sandbox's mount view of a file goes stale/truncates at a fixed byte offset after ANY same-session Write/Edit to an already-existing path (not just pre-existing-from-before-session files as previously documented) — Windows Read tool always shows correct full content; only brand-new file paths reliably sync in bash. Recorded as ADR-104 addendum #8.
Changed: `app/voice_agent/telecaller_brain.py` (`_kb_facts()` fully rewritten ~2796-2983, old `_get_kb()`/`_KB_SINGLETON`/`_KB_TRIED`/`_KB_LOADED_AT` removed, 8 new typed KB-state constants + `_kb_log_state()` redacted logger) · `app/voice_agent/kb_loader.py` (`replace_source=True` added to all 4 `add_documents()` call sites in `load_niche_faqs`) · `app/worker.py` (1-line `include=[]` registration, default queue, `task_routes` untouched) · `app/tasks/kb_niche_refresh.py` (NEW — `request_niche_refresh()` + `refresh_niche_task` Celery task, Redis SET-NX-EX lease + owner-token release, `autoretry_for`/backoff/jitter, verifies via `count_niche_catalog_points` post-seed) · `tests/test_kb_facts_adr104_v3.py` (NEW, 9 tests) · `tests/test_kb_niche_refresh_task.py` (NEW, 7 tests) · `memory/decisions.md` ADR-104 addendum #8. Two dead intermediate test-file drafts left as inert empty-docstring placeholders (`tests/test_kb_facts_adr104.py`, `_v2.py`) — see addendum #8 for why, should be deleted by a human.
Tests Run: could not run real `pytest`/`prod_check.py` in this sandbox (conftest.py needs `sqlalchemy`, pip installs timed out repeatedly — network flakiness in this session's sandbox only, unrelated to the repo). Built a manual test-execution harness instead (stubs `app.voice_agent.agent`/`app.worker` heavy imports, replicates pytest's `monkeypatch` fixture) and ran both new suites directly against the real, unmodified `app/voice_agent/kb_readiness.py` and the real, newly-created `app/tasks/kb_niche_refresh.py` — **16/16 passed**. The edited `telecaller_brain.py`/`kb_loader.py`/`worker.py` themselves could not be imported directly in-sandbox (mount staleness, see above); verified instead via (a) a byte-identical verification-copy shim (`app/voice_agent/_verify_telecaller_brain.py`, a NEW path, confirmed to sync + `ast.parse` clean) that the 9 `_kb_facts()` tests ran against, and (b) direct `Read`/`Grep` of the Windows-authoritative files confirming exact line-for-line match with what was implemented (no `_get_kb`/`_KB_SINGLETON`/`_KB_TRIED` live usage remains, `replace_source=True` present at all 4 sites, worker.py's `include` list syntactically closed correctly).
Verification Evidence: 16/16 test pass output captured in this session's tool transcript. Static guard test (`test_verify_module_does_not_import_bootstrap_symbols`) proves `_kb_facts()` contains no live import/call of `_get_qdrant_embedder`/`bootstrap_default_kb`/`_get_kb` (comment mentions explaining the fix's own history are explicitly allowed and distinguished from real usage). No `prod_check.py`/`check_secrets.py`/actual deploy run this session — explicitly out of scope per user's AskUserQuestion answers; these are listed in the runbook handed to the user.
Risks: **Nothing has been committed, pushed, or deployed.** The fix exists only in the local working tree. `git status` will show a dirty tree with PARALLEL unrelated uncommitted edits from other sessions (per ADR-104 addendum #7's working-tree-safety note) — the user's commit must stay surgical to the 6 files/paths listed in addendum #8, never `git add -A`. Two dead placeholder test files exist and should be deleted, not shipped as-is (they're inert but messy). The ~217k pre-existing duplicate Qdrant points from before this fix are NOT cleaned up (deliberately out of scope, destructive, addendum #7).
Remaining: user must review the diff, commit surgically, push, deploy with a real `APP_VERSION` SHA (ADR-097 gate — never `:latest`), then run two live Voice QA acceptance calls and confirm `/health` version + no executor-shutdown hangs. Exact commands to be handed over next.
Next Highest Priority: prepare the exact commit/push/deploy/acceptance-test command runbook for the user (Task #6), then (per the original task's continuation instruction) the admin status-truth fix is next once the user confirms this deploy landed clean.

## Loop Run
Date: 2026-07-15
Goal: Production admin audit via real browser — establish truth, verify admin surfaces, fix highest-priority provenance gap (Phase 4 #1).
Inspected: local git (HEAD/origin/dirty) · prod `/health` + `/health/ready` (browser + curl) · VPS containers/disk/mem/DLQ/celery/scheduler beat · `/app/control-center` + `/app/admin` (authenticated, real data) · `app/api/health.py` · `app/api/admin_dashboard_builders.py:598-603` · commit 388079c diff (deploy-risk review).
Problems Found: (1) **`/health` cacheable** — the FIRST browser GET returned a 12.7h-stale body (`version 91e7d37`, uptime 13m) while prod ran `b12d1e97`; `/health/ready` cached copy showed `version:"latest"`. No cache directives on any health endpoint (control-test: `/health/platform` → `Cache-Control=None`). CLAUDE.md designates this field THE drift detector. (2) **`estimated_mrr` has no payment-evidence gate** (`status=="active"` only) → headline `Est. MRR ₹8.0K` counts synthetic `Test Biz` (₹2,999, 0 invoices per ADR-095) + own tenant `leadgenai-self` (₹2,999); real MRR = ₹1,999 (Jiya). Same page shows contradictory ₹2.0K in Revenue Analytics. (3) Same synthetic tenants generate a false **"4 UPI payment activate karo — revenue ruka"** admin queue (all 4 rows = test numbers 9123456780 / 987654321x, all Trial ₹0). (4) `HEAD /health` → 404 while `GET` → 200.
Changed: `app/api/health.py` — `_mark_no_store()` on `/health`, `/health/live`, `/health/ready`, `/health/signup` (header-only, additive). New `tests/test_health_no_store.py`. ADR-100 (+ addendum) and ADR-101 in `memory/decisions.md`. No fix attempted for MRR (billing-truth touch = contract test FIRST, §6) — recorded as ADR-101 with the reuse path (`has_paid_evidence()`).
Tests Run: `pytest tests/test_health_no_store.py` → 5 passed. Discrimination proven against unfixed control endpoint (`/health/platform` → `Cache-Control=None`), i.e. the test fails without the fix. `prod_check.py` → ALL CHECKS PASSED (1102 routes, 47 pages 0 gaps). `check_secrets.py` → clean (72 changed files).
Verification Evidence: deploy_vps.sh → `=== DEPLOYED 685cffaa OK ===`; `/health` version `685cffaa` environment `production`; skew check all 5 containers `APP_VERSION=685cffaa`; smoke `/health`,`/api/voice/niches`,`/api/billing/plans`,`/api/public/pay-info` all 200; queues/DLQ 0/0; retention removed c84b62a6, disk 78% used 44G free. Prod header proof via Caddy: `cache-control: no-store, no-cache, must-revalidate, max-age=0`.
Risks: **Fix does NOT evict already-poisoned browser caches** (verified: after deploy, normal navigation STILL served `/health`→91e7d37 and `/health/ready`→`version:"latest"`; only hard-reload showed `685cffaa`, and the next normal nav reverted to stale). Operator browsers will keep lying until cache expiry/clear — and `version:"latest"` there is a FALSE ADR-097 alarm. SOP: verify `/health` with curl or `?cb=`, never a plain browser visit.
Remaining: MRR payment-evidence gate (user chose the `has_paid_evidence()` point-fix) · false "revenue ruka" queue rows · 293-post approval backlog · email warmup PAUSED (complaint 0.585% ≥ 0.25%, 143 pending) · own-brand posting still UNPROVEN (cockpit "published: 6" rows all have EMPTY post_id + dates 07-04/07-08 = pre-dry-run-flip fabrications) · reply agent classifies gambling spam ("Reddy Anna") as `interested` with draft ready · `HEAD /health` 404 · Phase 3 end-to-end Jiya delivery + Phase 5 Admin Guide not completed this session.
Next Highest Priority: gate `estimated_mrr` on `has_paid_evidence()` + contract test in `test_billing_truth_2026.py` — the founder's headline revenue number is 4x wrong and it is the SAME bug class as ADR-095/096 (synthetic tenants poisoning production metric/alert surfaces, now 3rd instance; consider the structural `is_synthetic` gate instead of a 4th point-fix).

## Ops
Date: 2026-07-14
Fact: Live VPS ANN-vs-exact probe (`docker compose exec app python scripts/rag_retrieval_ab.py`, embedder=paraphrase-multilingual-MiniLM-L12-v2 dim=384): ANN recall==exact recall (gap=0.0) → filtered-HNSW recall healthy, KB_HNSW_EF stays default. A/B gate: rerank/hybrid/full all PASS (+0.10 recall, +0.067 prec vs baseline on the 6-doc labeled set); gate recommends `rerank`.
Change: `USE_RERANKER` in /opt/leadgen/.env was ALREADY `=1` (line 372) — so the deploy-recreated container already runs rerank. Idempotently normalized to a single clean line (value unchanged). Backup `.env.bak.pre-rerank-20260714-080037`. Verified in running container: `printenv USE_RERANKER=1`, `reranker.rerank_enabled()=True`, `/health` version 8102a27 environment production, uptime 16m (no restart, zero downtime). Eval seeded throwaway `ab:*` namespaces only (isolated from real tenants).

=======
>>>>>>> 4d3f030 (fix: reconcile workspace + resolve conflicts + finalize ADR-100/103 fixes)
## Deploy
Date: 2026-07-16
Shipped: **`2f8bbb1c`** (ADR-105 spam guard) — deployed via canonical deploy_vps.sh, `=== DEPLOYED 2f8bbb1c OK ===`, zero skew (5/5 containers), all routes 200, workers 3/3 pong, queue 0.
Gates before ship: reply suites 64 passed · tenant isolation 19 passed · prod_check ALL PASSED (1125 ops) · check_secrets clean. Commit made in ISOLATED worktree `lg-adr105-wt` on origin/main (main tree's parallel dirty work untouched); exactly 4 files +160/-0.
Git reconciliation: the "phantom" staged-revert from 07-15 was REAL on Windows — index had -735-line revert + staged-DELETE of ADR-104 tests while disk was intact; fixed with `git restore --staged` on exactly those 10 files. Main tree still on `0350ee18` (behind origin) + parallel dirty work — next session: commit own work, then `git pull --ff-only`.
Postiz closeout: registration runtime-BLOCKED (POST register → 400 "Registration is disabled"); **own-brand posting end-to-end PROVEN** — post_id `cmrmx5ij50003nz6o1nuachza` facebook PUBLISHED 2026-07-16 02:57Z, releaseURL facebook.com/122101038657384404/posts/122110418541384404. X channel = credits depleted (402, external/paid). Hot-Queue spam filter runtime-proven on real drafts (1815 rows, 2 betting rows hidden, 1203 legit visible).
Remaining: jiya UI login owner spot-check (credential-gated) · DLQ 1 stale SIGKILL entry (job 263, 01:14Z pre-deploy) triage · mem 81.5% post-build watch · X credits + YouTube OAuth publish (owner) · worktree cleanup after review.
Rollback: `APP_VERSION=5f65979c bash scripts/deploy_vps.sh` (rebuilds from SHA) · spam guard alone: `REPLY_SPAM_CONTENT_GUARD=0`.

## Loop Run
Date: 2026-07-16 (Fix All Issues / Launch-Readiness)
Goal: Reconcile main working tree with origin/main, resolve conflicts in stashed parallel work, and finalize technical debt.
Inspected: main branch git status/diff (dirty tree behind origin) · progress.md (conflict markers) · memory/playbooks.md (conflict markers) · app/api/growth_automation.py · app/marketing/postiz_publish.py · tests/test_postiz_config.py.
Problems Found: (1) Main tree behind origin/main (`2f8bbb1c` lead) and dirty with parallel session fixes. (2) Multiple merge conflicts in core code/test/memory files after pull. (3) Sandbox git lock issues (`main.lock`).
Changed: (a) Removed git locks and conflicting untracked test file. (b) Reconciled `main` with `origin/main` (ff-only). (c) Resolved conflicts in 5 files (growth_automation.py, postiz_publish.py, test_postiz_config.py, playbooks.md, progress.md) — kept stashed parallel improvements (ADR-099, ADR-100, ADR-103) on top of production release. (d) Finalized `email_warmup.py` unsub-vs-complaint split (ADR-103) and `team.py` N+1 fix (ADR-100) in local committed state.
Tests Run: AST check all 5 resolved files (clean). `prod_check.py` on local resolved tree.
Verification Evidence: local HEAD now matches `origin/main` commit ancestry; all 5 unmerged paths resolved; stashed fixes for status surface and N+1 performance integrated.
Risks: Parallel work from multiple sessions is now merged — verify no functional regression in status reporting or email tracking.
Remaining: (1) Commit the resolved/merged fixes. (2) YouTube OAuth publish (owner action). (3) Unity WebGL artifact shipping (owner action).
Next Highest Priority: Final commit and push of the reconciled workspace.

## Loop Run
Date: 2026-07-15 (both-product launch baseline + Postiz proof hardening)
Goal: Dirty-tree-safe launch baseline ship karna, Postiz registration close karna, aur own-brand social canary ko provider evidence ke bina false-positive hone se rokna.
Inspected: isolated worktree/origin/main/VPS drift; canonical deploy script; five app-image services; Postiz compose/env/account DB; social queue, self-brand approvals, provider resolver; Postiz official create-post response contract; voice/compliance launch gates.
Problems Found: (1) live SHA `5f65979c` origin/main se 9 commits behind tha; VPS tree me customer/runtime data dirty tha. (2) Postiz public registration open tha. (3) six historical self-brand jobs `published` the par `post_id/post_url` empty tha. (4) current Postiz adapter successful API response ke `postId` values discard karta tha aur current API ka required `settings.__type` nahi bhejta tha, isliye plan ka real provider-proof acceptance impossible/fragile tha. (5) 18 approved self-brand items hain, par six historical jobs approval-id se linked nahi hain; blind canary duplicate post kar sakta hai.
Changed: clean branch se additive effective Postiz status diagnostics ship kiye; stale signup tests ko current contract ke saath hermetic banaya; canonical pinned deploy `f6fb352a`; timestamped Postiz env backup banakar `POSTIZ_DISABLE_REGISTRATION=true` aur documented compose restart apply kiya. Follow-up code now preserves Postiz `postId` list/primary id/URL evidence through `PostizProvider` and sends provider `settings.__type`; tests added. Customer/Jiya content aur voice calling untouched.
Tests Run: isolated `tests/test_postiz_config.py` (added: integrations_source / effective_integration_ids / postId-preservation / settings-type-matching) = **13 passed**. `prod_check` (clean branch) = **ALL PASSED** (1125 routes). Live Postiz probe: `POST register` -> 400 ✓.
Verification Evidence: ancestry: `f6fb352` lead. Postiz logs: `Registration is disabled` on public attempts. Postiz adapter AST: `postId`/`settings` logic verified.
Risks: trainer's root cause (tasks #3/#4) remains genuinely unconfirmed - disposition is "monitor only," not "fixed." If a 3rd/4th trainer dead-task appears, this needs real investigation, not another assumption that it's the same qa-class bug (confirmed it structurally cannot be, since run_trainer never touches the KB/brain path).
Remaining: Phase E (Jiya E2E via Claude in Chrome), F (admin setup gaps), G (browser training), H (Admin Operating Guide), Qdrant historical cleanup dry-run, final report.
Next Highest Priority: Phase E - verify Jiya Makeover end-to-end via the real authenticated admin interface (Claude in Chrome).

## Loop Run
Date: 2026-07-15 (both-product launch baseline + Postiz proof hardening)
Goal: Dirty-tree-safe launch baseline ship karna, Postiz registration close karna, aur own-brand social canary ko provider evidence ke bina false-positive hone se rokna.
Inspected: isolated worktree/origin/main/VPS drift; canonical deploy script; five app-image services; Postiz compose/env/account DB; social queue, self-brand approvals, provider resolver; Postiz official create-post response contract; voice/compliance launch gates.
Problems Found: (1) live SHA `5f65979c` origin/main se 9 commits behind tha; VPS tree me customer/runtime data dirty tha. (2) Postiz public registration open tha. (3) six historical self-brand jobs `published` the par `post_id/post_url` empty tha. (4) current Postiz adapter successful API response ke `postId` values discard karta tha aur current API ka required `settings.__type` nahi bhejta tha, isliye plan ka real provider-proof acceptance impossible/fragile tha. (5) 18 approved self-brand items hain, par six historical jobs approval-id se linked nahi hain; blind canary duplicate post kar sakta hai.
Changed: clean branch se additive effective Postiz status diagnostics ship kiye; stale signup tests ko current contract ke saath hermetic banaya; canonical pinned deploy `f6fb352a`; timestamped Postiz env backup banakar `POSTIZ_DISABLE_REGISTRATION=true` aur documented compose restart apply kiya. Follow-up code now preserves Postiz `postId` list/primary id/URL evidence through `PostizProvider` and sends provider `settings.__type`; tests added. Customer/Jiya content aur voice calling untouched.
Tests Run: billing/activation 70/70; Postiz/status 12/12 before baseline deploy; social/Postiz evidence group 31/31 after follow-up; signup auto-login 6/6; signup rate UX 3/3; voice compliance/campaign/customer-selfserve suites isolated green; `prod_check.py` PASS twice on final follow-up code (last run 121.1s, 1101 routes, 47 pages/0 gaps, Explorer 81/81); `check_secrets.py` clean; cross-path/duplicate-route/explorer sync clean.
Verification Evidence: baseline deploy script reported BUILD_RC=0, UP_RC=0, `/health.version=f6fb352a`, five-of-five APP_VERSION exact, four critical smokes 200, Celery/DLQ 0/0. Postiz DB has one existing user; backup `deploy/postiz/.env.backup-20260715T171929Z`; running container env `DISABLE_REGISTRATION=true`; public `/auth/register` redirects to `/auth/login`; main/Postiz DB/Redis/app/workers healthy. Effective publish config: enabled=true, API `https://postiz.leadsgenai.in/api`, four integrations from env, dry_run=false.
Risks: follow-up provider-evidence code is locally green but not yet committed/deployed in this loop entry. A real canary is deliberately not fired until a specific approved item can be proven not to duplicate a historical empty-evidence job. YouTube OAuth remains expired/operator-browser work. Real voice calls are outside the 09:00-19:00 safety window and need supervised approved numbers.
Remaining: commit/push/deploy the evidence follow-up; select a non-duplicate approved own-brand canary; verify Postiz post id plus external browser-visible post; Google OAuth production publish/reconnect; browser UAT; supervised voice pilot; monitoring reviews.
Next Highest Priority: evidence follow-up deploy karo, phir approval-to-job caption match se exactly one non-duplicate own-brand canary identify karo.

## Loop Run
Date: 2026-07-15 (launch closure evidence + guarded-voice stop)
Goal: Postiz provider-evidence follow-up deploy karna, real own-brand canary prove karna, browser funnel/admin UAT karna, aur guarded voice pilot ko acceptance gates ke against test karna.
Inspected: live SHA/skew/queues; Postiz API list + Post table; approved self-brand caption hashes vs historical jobs; Chrome pricing/start/voice/Facebook/admin automation/control-center; voice latency simulation + live web-call harness; runtime dial/selfserve/WhatsApp safety gates.
Problems Found: (1) first canary Facebook published hua par X child ERROR; cause X API 402 credits depleted (non-retryable). (2) Postiz create acceptance har child ke terminal state se pehle milti hai, so partial failure needs reconciliation. (3) voice simulation p95 2.56s POOR; full live suite 364s timeout; isolated solar strict FAIL with missing opener, 2/4 missed replies, p95 13.13s. (4) guide ka Customer 360 Deliver-Now "not fixed" note stale tha; current source has the red confirmation modal. (5) four historical dlq:dead records remain visible, with no launch-run growth.
Changed: commit `972bd74` preserves provider post IDs/URLs and Postiz `settings.__type`; canonical five-service deploy green. First partial canary ke stop-condition par effective integrations reversible `.env` backup ke saath Facebook+Instagram tak reduced (X credits depleted, YouTube stale OAuth hard-off), then same pinned SHA canonical redeploy. Second approved non-duplicate canary cleanly Facebook PUBLISHED hua. Admin guide stale safety note corrected. Voice/public Marketing funnel rollback nahi kiya; platform dialing remains hard-off.
Tests Run: social/Postiz 31/31; final `prod_check.py` PASS (121.1s); secrets clean; live canary provider reconciliation; offline latency 20 runs; live web-call full + isolated solar strict gate.
Verification Evidence: `/health.version=972bd74`; five-of-five services exact SHA; four critical smokes 200; Celery/DLQ 0/0; activation launch=true/payments=true/blockers=0/warnings=0. Registration lock persisted. Clean canary approval `dba99c55c892` -> job `46d14cc39a394ff3` -> Postiz/Facebook id `cmrmdurh50002nz6o14axedjl`, state PUBLISHED, non-empty release URL, Chrome title rendered the Leadsgenai post; only one matching provider child and no duplicate/error. Browser proved ₹1,999/₹5,999 cards, `/start` free-trial form fields, standalone `/voice-agent` WhatsApp flow, admin Automation + Control Center truth surfaces. Voice gate evidence is an explicit FAIL, not hidden.
Risks: Postiz create call remains asynchronous; future multi-channel child failures need a reconciliation monitor. X needs credits or permanent exclusion; YouTube needs OAuth production publish/reconnect before re-add. Voice first-turn/cold-KB path is far above pilot latency target. Four historical dead records remain triaged but present. Customer signup submit/auto-login was test-proven but not browser-submitted because no user-owned test email/password was supplied.
Remaining: voice NO_GREETING/NO_REPLY/13s root cause fix and re-score; supervised calls only 09:00-19:00 with owner-provided allowlist; Google OAuth production publish + YouTube reconnect; credential-owned Postiz login click-through; 24h/72h monitoring; optional fresh browser re-walk of `/app/clients` after browser-control reset.
Next Highest Priority: Voice guarded pilot ko HOLD rakho; web-call opener/cold-KB timeout root-cause fix ke baad strict scorecard re-run karo, phir hi supervised owner calls.
