# =============================================================================
# CI â€” automated quality gate (SaaS-infra: "Infrastructure â†’ CI/CD" pillar).
# Pehle koi CI nahi tha (tests sirf manual run_tests.bat se deploy se pehle).
# Ab har push/PR pe GitHub Actions automatically gate chalata â€” broken code main
# tak na pohnche. GitHub-hosted runner (free) â€” VPS deploy isse ALAG (woh manual
# git-pull + docker rehta, ye sirf pre-merge quality gate hai).
# =============================================================================
name: CI

on:
  push:
    branches: [main]
    paths-ignore: ["docs/**", "**/*.md", "**/*.bak"]
  pull_request:
    branches: [main]
    paths-ignore: ["docs/**", "**/*.md", "**/*.bak"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Fast, dependency-light gate â€” hamesha reliable (koi heavy ML dep nahi).
  quality:
    name: Lint + syntax + secrets
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install ruff
        run: python -m pip install --upgrade pip ruff
      - name: Syntax check (compileall)   # MUST-PASS â€” syntax error = red
        run: python -m compileall -q app scripts
      - name: Secret scan                 # MUST-PASS â€” no secrets in commit
        run: python scripts/check_secrets.py
      - name: Security misconfig scan     # MUST-PASS â€” no misconfigurations in commit
        run: python scripts/security_scan.py
      - name: Queue idempotency audit     # RATCHET â€” static-scan gaps > baseline = fail (naye tasks bina dedup na aayen)
        env:
          # 2026-07-06 baseline: 117 static gaps (kai tasks ka dedup scheduler/
          # day-key layer pe hai jo heuristic nahi dekhta). Naya gap = RED.
          IDEM_AUDIT_MAX_GAPS: "117"
        run: python scripts/queue_idempotency_audit.py
      - name: Dependency vulnerability scan  # MUST-PASS â€” no known CVEs in dependencies
        run: |
          pip install pip-audit
          pip-audit --requirement requirements.txt --format=json || true  # advisory until lock file refreshed
      - name: Type check (mypy)            # advisory â€” 1522 pre-existing errors (2026-07-06); type-debt visible, merge-gate nahi
        run: |
          pip install mypy
          mypy app --ignore-missing-imports --no-error-summary || true
      - name: Ruff lint (repo config)     # informational annotations (style debt block na kare)
        run: ruff check --output-format=github app || true

  # Full gate â€” runtime deps (Dockerfile.lock-mirror) + prod_check + pytest.
  # `-m "not network"` = LLM/internet tests skip (repo marker, no API keys CI me).
  tests:
    name: prod_check + pytest
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: System libs (voice/audio imports)
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends ffmpeg libsndfile1
      - name: Install Python deps (mirrors Dockerfile.lock)
        run: |
          python -m pip install --upgrade pip wheel setuptools
          if [ -f requirements.lock.txt ]; then
            grep -v -E "^(playwright|selenium)" requirements.lock.txt > req.filtered || cp requirements.lock.txt req.filtered
            pip install --no-deps -r req.filtered
          else
            pip install -e ".[dev,monitoring]"
          fi
          # torch CPU (Dockerfile.lock jaisa) + test tooling (lock me na ho to).
          pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu || true
          # PINS (tests.yml gotcha): unpinned pytest-family + newer httpx (0.28
          # removed Client(app=...)) collection-errors dete the.
          pip install -U "pytest==9.0.2" "pytest-asyncio==1.3.0" "pytest-timeout==2.4.0" "httpx==0.27.2"
      - name: Production readiness check
        run: python scripts/prod_check.py
      - name: Pytest (skip network/LLM-key tests)
        env:
          APP_ENV: test
          ENVIRONMENT: development  # local-dev parity (prod-default middleware OFF)
          # TelecallerBrain __init__ key-presence check â€” dummy kaafi (LLM calls
          # conftest-stubbed, no network).
          GROQ_API_KEY: ci-dummy-key-not-real
        run: pytest -m "not network" -q --no-header -p no:cacheprovider --timeout=60
