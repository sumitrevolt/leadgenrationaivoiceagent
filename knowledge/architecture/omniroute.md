---
type: Architecture
title: OmniRoute
description: Local-dev LLM gateway adapter — INERT on VPS unless flags ON.
tags: [omniroute, llm, free-stack]
timestamp: 2026-07-17T00:00:00Z
resource: app/platform/omniroute_client.py
---

# OmniRoute

- Local WSL gateway only by default; VPS flags OFF.
- Routes use free combo / auto-aliases (`leadgen-free-first` + fallbacks) — free-tokens mandate.
- **2026-08-16: `leadgen-project-best` = flagship-free 16-step chain (rebuild; old DeepSeek-only chain backed up at `/root/.omniroute/backup-combo-leadgen-project-best-before-flagship.json`)** — order: Antigravity Claude Opus 4.6 ×2 → Gemini 3.1 Pro ×2 (Antigravity/AGY) → Codex GPT-5.5 → Kiro Claude Haiku 4.5 → NVIDIA GLM-5.2 → Kiro GLM-5 → Gemini 3.1 Pro/3.5 Flash (direct) → gpt-oss-120b (Cerebras/Groq) → Mistral Large/Code → Groq Llama → Gemini Flash tail. Every step smoke-verified 200 on 2026-08-16 as local gateway/free-token aliases only; this is documentation of local OmniRoute routing, not a production paid-provider authorization. DSH (`C:\Users\Ratanshila\.dsh\settings.yaml`) binds this name — no config change needed.
- Dead/excluded until user action: GitHub Models (`gh/*`, quota exhausted ~15.6d), Vercel AI Gateway (`vcg/*`, needs credit card on file), cline/kimi-coding/openrouter/hfr (creds expired/exhausted), nvidia/deepseek-v4-pro (410 removed from NVIDIA roster), gemini/gemini-3-pro-preview (404 removed).
- Combo management = `POST /api/combos` (create), `DELETE /api/combos/{uuid}` (delete; CLI delete auth-broken), `GET /api/combos` — all with Manage API key (`Authorization: Bearer ${OMNIROUTE_MANAGE_API_KEY}`, stored in `api_keys` table).
- Provider label for combo IDs = `combo` (or resolved `provider/model`), not a fake provider name.
- **2026-08-23: bare-OpenAI-model landmine** — gateway pe koi `codex` provider connection NAHI hai (0/40); koi bhi bare OpenAI model-id (`gpt-5.6-sol`, `gpt-5.3-codex`) `/v1/responses` pe built-in `codex` provider pe resolve hokar `404 "No active credentials for provider: codex"` deta hai. Combo ids (`leadgen-project-best`) = 200. ChatGPT/Codex desktop app (`~/.codex/config.toml`, `[desktop]` section shared) ka top-level `model` isi wajah se `leadgen-project-best` pin kiya gaya — desktop restart zaroori hai config reload ke liye; codex exec end-to-end verified (CODEX_OMNI_OK, session 01a02dee).
- **2026-09-06: ChatGPT/Codex desktop gateway wiring REMOVED (owner request)** — `~/.codex/config.toml` ab `model = "gpt-5.6-sol"` + `model_provider = "openai"` (user's own OpenAI profile; `auth.json` tokens present); `[model_providers.omniroute]` block deleted (0 omniroute refs, TOML-validated). Rollback = `~/.codex/config.toml.bak-omniroute-removal-20260906` (ya older `.bak-pre-omniroute-wire-20260823-140250`). Opt-in `~/.codex/omniroute.config.toml` (alt config) abhi bhi pada hai — active NAHI; `--config` se hi use hota.
- **2026-09-06: desktop-app gateway AUDIT (live config scan, docs se nahi)** — kaun sa app gateway pe point karta hai + kya INTENTIONAL hai (31-agent workforce fleet = repo ki apni apps, `scripts/sync_all_combos_all_apps.py` 14-combo matrix + `start-all-combos-desktop.ps1` se deliberately synced; free-stack mandate = combos free-provider chains, isliye fleet ₹0). ChatGPT/Codex desktop hi personal paid profile tha → akele revert kiya. Per-app truth table:
  | App (config file) | Gateway refs | Default model/provider | Verdict |
  |---|---|---|---|
  | DSH (`~/.dsh/settings.yaml`) | `baseURL :20128/v1` | `leadgen-coding-primary`/`omniroute` | gateway — INTENTIONAL |
  | Claude Code CLI (`~/.claude/settings.json`) | `ANTHROPIC_BASE_URL :20128` | `claude-code` (env) | gateway — INTENTIONAL |
  | WorkBuddy (`~/.workbuddy-ai/settings.json` + `models.json`) | omniroute `:22000`; 14 combos `:22000`+`:20128` | 14 combos | gateway — INTENTIONAL ⚠️ proxy down |
  | Hermes (`~/AppData/Local/hermes/config.yaml` + cache) | `:20128/v1` (custom `:22000`) | `leadgen-coding-primary`/`omniroute` | gateway — INTENTIONAL |
  | OpenClaw (`~/.openclaw/openclaw.json`) | omniroute `:20128`, custom `:22000` | combos | gateway — INTENTIONAL |
  | Verdant (`~/.verdant/settings.json`+`config.json`) | activeProvider omniroute `:20128`, claude_proxy `:22000` | combos | gateway — INTENTIONAL |
  | Claude Desktop (`~/AppData/Roaming/Claude/`) | ❌ NONE (settings = tools/perms; config = MCP only) | Claude subscription | NOT wired |

  **Findings:** (1) **port 22000 `claude_proxy.py` DOWN** at audit time — sirf `:20128` (Docker `com.docker.backend` + `wslrelay`) listen kar raha tha; WorkBuddy ka `settings.json` omniroute provider `:22000` pe point karta hai → proxy bina `start-all-combos-desktop.ps1`/`claude_proxy.py` start kiye unreachable (models.json ke `:20128` entries se fallback possible). (2) Gateway ports pe **0 ESTABLISHED connections** — sab apps idle (normal; koi active request nahi). (3) Docs/orchestrator ka "Claude Desktop ACTIVE (Claude Proxy :22000)" claim **STALE** — Claude Desktop config kabhi gateway pe gaya hi nahi; `Roaming/Claude/` me stray `claude_proxy.py` + log = mangled `C:\\c\\...` path se failed launch attempt.
- **2026-09-06: stray `Roaming\Claude\claude_proxy.py` + `ClaudeProxy.vbs` CLEANED UP (owner-approved)** — chain: (1) 2026-08-28 Hermes agent runtime (`generation-1787125817-*\python.exe`) ne mangled Git-Bash path `C:\\c\\Users\\...\\claude_proxy.py` se launch kiya → `[Errno 2]` (file waha tha hi nahi) → `claude_proxy_windows.log`; one-off agent galti, koi persistent process nahi. (2) 2026-08-30 OLD 12-combo copy (hardcoded OmniRoute API key, prefix `sk-18ef…`, truncated — **rotation advised**) `Roaming\Claude\` me place hui. (3) 2026-08-31 `Startup\ClaudeProxy.vbs` bana → har login pe `pythonw.exe` se wo copy chalta tha. **pythonw + is proxy = DEAD LISTENER**: process port 22000 bind karta tha par HTTP serve nahi karta (python.exe se chalta hai to `/health` 200 deta hai) → autoboot probes "connection closed unexpectedly" (Sep-1/Sep-2 boots) + LeadGen_AutoBoot race. Cleanup: teeno files removed; canonical `scripts\claude_proxy.py` ab single source (autoboot `python.exe` method = proven); proxy abhi `python.exe` hidden se 22000 pe chalta hai (pid verified, `/health` 200, 14 combos).
- **2026-09-06: LOCAL E2E verified + master-key grant (this computer)** — (1) **ROOT CAUSE of "API key policy rejected" (Hermes apps etc.):** OmniRoute Master Key (har app use karta hai — WorkBuddy/OpenClaw/Hermes auth me hash-verified `62fabd63…`; DSH/Verdant env se) sirf `leadsgen combo 14` allowed tha → ab **52 combos** (14 canonical + 38 aliases incl. `hermes-*`, `claude-omni-*`, `leadgen-*`, `vps-*`, `claude-code`) grant kar diye. DB backup `/app/data/db_backups/storage.sqlite.pre-mastergrant-20260906`. Live proof: `:20128` POST `leadgen-coding-primary` (combo 1) + `hermes-voice` (combo 6) dono 200 real completions (nemotron-3.5-lightning-free lane); `:22000` proxy `claude-omni-coding-primary` bhi 200. Per-worker email keys ab bhi apne combo pe scoped hain (user ChatGPT-computer-use step unhe apps me dalega → quota separation). (2) **Proxy**: single canonical instance `python.exe scripts\claude_proxy.py` :22000 (pid verified, `/health` 200, 14 combos). (3) **Memory**: host 15.8GB, 91% used — WorkBuddy ×3 full instances (09-03/09-04/09-06) + heavy Hermes agent runtimes + 10 desktop app stacks; safe close-list owner action. (4) **Network**: no proxy env, WinINET off; host→internet ~4.5-5s (Google/Pollinations) = ISP latency; gateway egress health checks (nvidia/pollinations/sambanova/opencode/ollama-cloud) isi se 8s+ timeout karte hain — local `:20128`/`:22000`/`localhost` sab fast (10-70ms) — desktop apps isse unaffected.
- Fail-open: gateway miss → existing `free_ai` chain continues.
- Never send raw customer PII — `mask_customer_data` + `validate_no_secrets` before network.
