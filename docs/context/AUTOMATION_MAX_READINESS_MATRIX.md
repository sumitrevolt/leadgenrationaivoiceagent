# Automation Max Readiness Matrix

> **Scope:** Code-and-evidence audit of the Master Blueprint (`app/platform/blueprint_graph.py` + `blueprint_detail_nodes.py`) against the automation-flag registry (`app/api/automation_flags.py`), scheduler contract (`app/platform/scheduler_config.py`, `app/tasks/staff_jobs.py`), and the mounted admin/customer surfaces. No live-host claim is made from this file alone — read `/health` and `/api/blueprint/graph` for runtime status.
>
> **Snapshot ref:** worktree `C:\Users\Ratanshila\Documents\_leadgen_worktrees\lg-cutover-complete` @ `58a3b70` (post PR #188 merge; prod is mid-rollout from `6b1dabb2` → `58a3b70` at the time of writing).
>
> **Blueprint truth (code-derived after drift repair):** `L0 = 48`, `L1 = 8`, `L2 = 1`, total nodes = **57**. Edges = 52. Flows = 11. Layers = 9. Domains = 18. Workforce count = registry-derived from `app.platform.team.STAFF` (Boss/OpenClaw = manager persona inside that registry, not a 32nd row). Added L1: `detail_sales_autopilot`, `detail_creative_os`, `detail_owner_email_canary` (CODE-PRESENT / inert — not live).
>
> **Automation-flags registry:** `app/api/automation_flags.py:9-391` lists **250+ named flags** in one Python list. The only admin JSON exposure is `/api/growth/infra/flags` (`app/api/growth.py:1446-1468`); there is no dedicated `/api/automation-flags/*` router.
>
> **Scheduler registry:** `app/platform/scheduler_config.py:37-204` registers **43 jobs** in `JOB_META`. `RUN_DUE_EXCLUDE` (line 211) permanently excludes `platform_dial`, `email_outreach`, `email_followup`, `digest`, `sales_autopilot` from run-due recovery (side-effect-heavy jobs must not double-fire).

## Label vocabulary (as agreed with user)

- **PRODUCTION-PROVEN** — code + tests + user-declared LIVE in prod (from CLAUDE.md hot facts / this task's context header). This file cannot confirm prod on its own; the label is inherited from that declared context and is falsifiable by `/health`.
- **TEST-PROVEN** — pytest module directly exercises the capability (`tests/test_*.py`).
- **CODE-PRESENT** — real files exist and the capability is import-safe, but no user-declared LIVE or dedicated test row.
- **CONFIGURED-INERT** — code exists, flags are wired, defaults are OFF and the runtime path is a graceful no-op until an operator flips the flag.
- **OWNER-ACTION-REQUIRED** — the code side is done; the last mile is a human decision (key rotation, DLT paperwork, external approval, canary green-light).
- **HARD-OFF** — permanently disabled by policy (compliance / ban-safety); not eligible for enablement without a documented policy reversal.

## Readiness matrix

| # | Capability | State | UI / API / Scheduler / Worker wiring | Required configuration | Owner (agent / role) | Safety gate | Test evidence | Runtime evidence (code-cited) | Remaining blocker |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Ops watchdog (Kavya scheduler / DLQ) | **PRODUCTION-PROVEN** | `app/platform/ops_watchdog.py:230` · scheduler `ops` daily / `watchdog` hourly:35 (`scheduler_config.py:44,50`) | `OPS_WATCHDOG=1` | Kavya | Fail-open, records only | `tests/test_ops_watchdog.py` | User-declared LIVE (task header + `## Current State`); no explicit host check inside this repo | Nothing (baseline live) |
| 2 | Cadence engine | **PRODUCTION-PROVEN** | `app/marketing/cadence.py:70,201` · driven by scheduler `growth` and in-process tick (`team_scheduler.py:515`) | `CADENCE_ENGINE=1` + `AUTOMATION_FLAGS` registry entry | Boss (rollup) | Only advances due steps, no send | `tests/test_cadence_run_due_active_limit.py` | User-declared LIVE | Nothing |
| 3 | Journey engine | **PRODUCTION-PROVEN** | `app/marketing/journeys.py:47,206,334` · `app/api/journeys.py` router | `JOURNEY_ENGINE=1` (need ≥1 inquiry rule) | Boss | Empty-loop when flag off; per-rule gate | `tests/test_2026_features.py` (journey rules) | User-declared LIVE | Nothing |
| 4 | Approval-email sweep | **PRODUCTION-PROVEN** | `app/platform/approval_notifier.py:548` · scheduler `approval_email_sweep` hourly:40 (`scheduler_config.py:189-193`) | `APPROVAL_EMAIL_NOTIFY=1`; `APPROVAL_EMAIL_NOTIFY_HARD_OFF` overrides fail-closed | Platform | Fail-closed allowlist (`APPROVAL_EMAIL_CLIENT_ALLOWLIST`) | `tests/test_approval_notify_scheduler.py`, `tests/test_approval_notification_migration.py`, `tests/test_approval_sweep_counter_truth.py` | User-declared LIVE | Nothing |
| 5 | Owner-inbox email canary | **CODE-PRESENT (deploy in flight)** | `app/api/owner_email_canary.py:17,27,32,52` (`/api/admin/owner-email-canary/{preflight,send,last}`) · super-admin one-shot | Super-admin token; runtime-data allowlist entry declared | Platform | One-shot, ledgered, TOCTOU-safe | `tests/test_owner_email_canary.py` | Merged as PR #187 (`6b1dabb2`); rolling out with `58a3b70` per task header | Runtime `/health` proof after `58a3b70` finishes deploy |
| 6 | Auto email outreach (Rohan) | **CONFIGURED-INERT** (default) | `app/platform/auto_outreach.py` · scheduler `email_outreach` hourly 9-19:05 (`scheduler_config.py:67-71`), excluded from run-due catch-up | `OUTREACH_ENABLED=1` / `AUTO_EMAIL_OUTREACH=1`; `EMAIL_WARMUP=1` recommended; 25/day cap, 60/run harvester cap | Rohan | Cap + warmup + `HARVEST_INGEST_VALIDATION` | `tests/test_workflow_gaps.py`, `tests/test_apollo_inspired.py` | Registered in `JOB_META`; scheduler beat entry exists | Owner sign-off + warmup ramp; `HARVEST_INGEST_VALIDATION` must stay 1 |
| 7 | Reply triage agent (draft-only) | **CODE-PRESENT** | `app/platform/reply_agent.py` · scheduler `reply_triage` hourly:20 (`scheduler_config.py:45-49`) | `REPLY_AGENT=1` for scoring; `REPLY_AUTO_SEND` HARD-OFF stays 0 | Rohan | `REPLY_AUTO_SEND_HARD_OFF=1` blocks sends regardless | (indirect via workflow tests) | Beat entry present; body draft-safe | Owner never flips `REPLY_AUTO_SEND` until deliverability proven |
| 8 | Prospector / lead harvester | **CONFIGURED-INERT** (throttled) | `app/platform/prospector.py`, `app/platform/lead_harvester.py`, `app/platform/niche_prospector.py` · scheduler `prospect` daily 09:30, `midday_prospect` 14:30, `evening_prospect` 17:00 (`scheduler_config.py:111,146,156`) | `LEAD_HARVESTER=1`, `GTM_TARGETING=1`, `UDYAM_PIPELINE=1` (all optional additive), `PROSPECT_MAX_LOOKUPS=60` | Rohan | ToS-safe sources only (`ALLOW_TOS_SCRAPE=0` HARD-OFF), Maps quota | `tests/test_workflow_gaps.py` (indirect) | Scheduler slots exist for 3 daily passes | Ops decides which passes to arm; keep `ALLOW_TOS_SCRAPE=0` |
| 9 | Content generation (Isha) | **CODE-PRESENT** | `app/api/contentauto.py`, `app/api/growth_content.py` · scheduler `content` 07:00, `blog` 06:30, `afternoon_content` 15:00 (`scheduler_config.py:93-98,151`) | `CONTENT_APPROVAL_AUTO=0` (submits to approval only), free-LLM chain up | Isha | Draft-only into approval queue | `tests/test_content_approval_state_machine.py`, `tests/test_content_approval_evidence_url.py` | Beat entries live | Owner routes to approval queue reviewer |
| 10 | Social publish (Postiz own-brand) | **CODE-PRESENT** | `app/api/social_oauth.py`, `app/platform/brand_pulse.py`, `app/social_engine/*` · scheduler `social_drain` hourly:10 (`staff_jobs.py:146`) | `SOCIAL_ENGINE=1`, `POSTIZ_INTEGRATIONS`, `SOCIAL_DRY_RUN=0` for live; own-brand only until Meta review for customers | Isha | `SOCIAL_DRY_RUN` fabricates `dry-*` post ids when set (ADR-098) | `tests/test_social_prefs_honored.py` | Beat entry present | Meta review for customer Pages (external-blocked); own-brand ready |
| 11 | Video ad cycle | **CONFIGURED-INERT** | `app/marketing/video_ad_cycle.py`, `app/tasks/video_jobs.py` · scheduler slot inside content pipeline | `VIDEO_AD_CYCLE=1`, `VIDEO_PRODUCTION_ENABLED=1`, `VIDEO_CUSTOMER_REVIEW_ENABLED=1`, `VIDEO_SOCIAL_PUBLISH_ENABLED=1` | Isha / Boss | Reject-terminal + exact-hash approval (ADR-142) | `tests/test_video_approval_*` (8 tests) | Master flag off; approval saga tests green | Jiya canary owner login pending (per `## Current State`) |
| 12 | Creative OS (ADR-143) | **CONFIGURED-INERT** (on-disk, not in Master Blueprint) | `app/marketing/creative_os/service.py:36-539` · `CREATIVE_OS_ENABLED` gate returns `{"ok":false,"error":"CREATIVE_OS_ENABLED off"}` at every entry | `CREATIVE_OS_ENABLED=1` + provider flags (`CREATIVE_PROVIDER_*`), tenant + GPU budgets; providers currently return `provider_unavailable` | Isha / Boss | Deterministic FFmpeg fallback; no external model downloads; consent + provenance required | `tests/test_creative_os.py`, `tests/test_creative_os_brief.py` | ON DISK but INERT (per `## Current State` and `.cursor/rules/creative-automation-os.mdc`) | **Not represented in `blueprint_graph.py`** — canonical drift (repair patch #2 below) |
| 13 | Sales Autopilot canary | **CONFIGURED-INERT** (on-disk, not in Master Blueprint) | `app/platform/sales_autopilot/send.py`, `app/api/sales_autopilot_admin.py:28,57-164` (`/api/sales-autopilot/{summary,policy,prospects,attempts,eligibility/preview,run-canary,inbound/classify,seed-estique}`) · scheduler `sales_autopilot` hourly:25 (`scheduler_config.py:199-203`) but in `RUN_DUE_EXCLUDE` (line 211) | `SALES_AUTOPILOT_ENABLED=1`; per-channel `SALES_AUTOPILOT_{WHATSAPP,EMAIL}_ENABLED=1`; `SALES_AUTOPILOT_DRY_RUN` can only make it safer | Platform | Fail-closed; separate from `WHATSAPP_AUTO_SEND`; calling stays HARD OFF | `tests/test_sales_autopilot_{admin,eligibility,estique,flags,followup_reply,messages,scheduler,send}.py` (8 test files) | JOB_META entry + admin router mounted (`main.py:803-807`) | **Not represented in `blueprint_graph.py`** — canonical drift (repair patch #1 below) |
| 14 | Self-improve loop | **CODE-PRESENT** | `app/agents/self_improve.py` · scheduler `self_improve` in-process every tick (`team_scheduler.py:475-486`), `self_improve_tick` Celery task (`staff_jobs.py:176`) | `SELF_IMPROVE_LOOP=1`, `SELF_IMPROVE_APPROVAL=1` recommended, LLM budget guards | Boss | `eval_gate` observe-only until `EVAL_GATE_HARD=1` | `tests/test_self_improve_cap_watchdog.py` | Tick wiring is live-safe (falls back if Celery off) | Owner approval gate before enabling widely |
| 15 | Agent runtime (Kavya/Isha/Zara canary) | **CONFIGURED-INERT** | `app/platform/agent_runtime.py` referenced by blueprint node `agent_runtime` | `AGENT_RUNTIME=1` (+`AGENT_RUNTIME_LLM=1` for LLM draft-brief) | Kavya | RED lane always blocked; safety-lane in blueprint | (contract via `blueprint_graph.validate_graph`) | Blueprint node present; runtime OFF | Owner approves canary pilots |
| 16 | Flow runner / Process engine | **CODE-PRESENT** | `app/api/growth_process.py`, scheduler `flow_cron` every 5 min (`scheduler_config.py:43`), `process_autostart` daily 11:30 (line 141-145) · `process_tick` Celery task (`staff_jobs.py:306`) | `FLOW_RUNNER=1`, `FLOW_AUTO_TRIGGERS=1`, `PROCESS_ENGINE=1`, `PROCESS_AUTOSTART=1` | Platform | Draft-safe by default; per-flow triggers guarded | `tests/test_growth_automation_approvals_cache.py` (indirect) | Beat entries wired | Ops enables per surface |
| 17 | Automation health aggregator | **CODE-PRESENT** | `app/platform/automation_health.py:114,207,262,284` (`record_run/run_history/queue_depth/health`) · surfaced via `/api/growth/infra/*` and admin dashboards | `AUTOMATION_HEALTH_ALERTS=1` optional | Kavya | Fail-open, read-only | `tests/test_automation_health_clock_injection.py`, `tests/test_automation_health_dlq_dead.py` | Consumed by `blueprint_graph` node `automation_health` runtime key | Nothing (already observable) |
| 18 | Team roster / staff jobs registry | **PRODUCTION-PROVEN** | `app/platform/team.py`, `app/tasks/staff_jobs.py:165-347`, blueprint node `team_roster` derives count from `STAFF` | 31 staff registered; Boss/OpenClaw = manager persona in same registry | Boss | Registry-derived (no hardcoded workforce number) | `tests/test_blueprint_graph_contract.py`, `test_blueprint_hierarchy_v4.py`, `test_blueprint_ownership_import.py` | Blueprint refuses fabricated count when registry import fails (`blueprint_graph.py:44-71`) | Nothing |
| 19 | Master Blueprint graph API | **TEST-PROVEN** | `app/api/blueprint.py:34-138` (`/api/blueprint/{graph,public,validate,trace,meta}`) · consumed by `/app/explorer` | Admin auth for full graph, public payload sanitized | Platform | Public payload strips modules/flags/paths (`blueprint_graph.py:1208-1247`) | `tests/test_blueprint_api_auth.py`, `test_blueprint_derive.py`, `test_blueprint_edge_reconcile.py`, `test_blueprint_reconcile.py`, `test_blueprint_hierarchy_v4.py`, `test_explorer_blueprint.py`, `test_office_blueprint_shell.py` | 7 blueprint tests; `validate_graph(strict_files=True)` runs as CLI check | **Drift vs code:** sales_autopilot, creative_os, owner_email_canary not represented (patches 1-3 below) |
| 20 | Automation flags admin surface | **CODE-PRESENT** | `/api/growth/infra/flags` (`app/api/growth.py:1446-1468`) is the ONLY admin JSON view; `app/api/automation_flags.py` is registry-only, no router | Admin auth (`require_admin`) | Platform | Secret-suffixed flag values masked | (no dedicated flags-endpoint test) | 250+ flag names enumerated in one list | **No `/api/automation-flags` router**, no automation.html tab for flag toggles (patch #5 below) |
| 21 | WhatsApp automation | **PRODUCTION-PROVEN (1-click send only)** | `app/integrations/whatsapp.py`, `app/api/whatsapp.py`, WAHA session `default` linked (per `## Current State`) | `WHATSAPP_PROVIDER=cloud` or `waha`; **`WHATSAPP_AUTO_SEND` stays 0** | Isha | Ban-safety: bulk auto-send FORBIDDEN | (integration path used by workflow tests) | Blueprint node `whatsapp` PRODUCTION-PROVEN | `WHATSAPP_AUTO_SEND` HARD-OFF per policy; leave alone |
| 22 | Reply auto-send | **HARD-OFF** | `app/platform/reply_agent.py` guarded | `REPLY_AUTO_SEND_HARD_OFF=1` overrides always | Rohan | Fail-closed | (guarded by hard-off flag) | Guarded path exists | Do NOT enable without deliverability proof |
| 23 | Platform dial (cold outbound) | **HARD-OFF** | `app/platform/platform_dial.py` · blueprint node `platform_dial` `disabled=True` (validator enforces `blueprint_graph.py:1508-1511`) | `PLATFORM_DIAL_DAILY=0` + `data/platform_dial.json enabled:false` + scheduler override paused (3-layer kill) | Swara | User-mandate 2026-07-05: HARD OFF | (blueprint validator gate) | Blueprint validator refuses enablement | Do NOT re-enable |
| 24 | Voice launch campaign | **CONFIGURED-INERT** | `app/telephony/voice_launch.py` (`VOICE_LAUNCH_CAMPAIGN` master gate, `VOICE_LAUNCH_KILL` global kill) | `VOICE_LAUNCH_CAMPAIGN=1`, `VOICE_LAUNCH_KILL=0`, `VOICE_DAILY_CALL_CAP` ≤ 100, DLT approved | Swara | AI-disclosure, DND fail-closed, 9-19 IST window, recording retention 90 days | (voice compliance tests separate) | Master gate off; blueprint keeps voice FROZEN this wave | DLT approved per `## Current State`; still gated behind `platform_dial` HARD-OFF |
| 25 | KB / RAG refresh | **CODE-PRESENT** | `app/platform/kb_refresh.py`, blueprint node `kb_refresh` · scheduler `kb_refresh` Sun 05:00 (`scheduler_config.py:177`) | `KB_PREWARM=1`, `KB_WEEKLY_REFRESH=1` optional | Meera | Off-loop load; hard timeout | (KB tests indirect) | Beat entry live | Nothing |
| 26 | Skill library (Qdrant skills) | **CODE-PRESENT** | `app/platform/skill_library.py`, blueprint node `skill_library` | `SKILL_PACK=1`, `SKILL_PACK_KB_INGEST=1` (opt-in) | Meera | Ingest cost-gated | (indirect) | Node present | Owner enables ingest |
| 27 | UPI auto-activation | **HARD-OFF (by policy)** | `app/api/upi_payments.py`, `app/platform/upi_payments.py` · blueprint node `upi_payments` PRODUCTION-PROVEN (manual path) | `UPI_AUTO_ACTIVATE=0` (per `## Current State` 2026-07-18 billing containment) | Platform | Manual admin approval required | `tests/test_billing_truth_2026.py` | Manual UPI is the primary payment path | Auto-activation stays OFF until reconcile confidence proven |
| 28 | Customer webhooks (Svix-style) | **CONFIGURED-INERT** | `app/api/customer_webhooks.py`, blueprint node `webhooks` | `CUSTOMER_WEBHOOKS=1`, `CUSTOMER_WEBHOOK_DENY_PRIVATE=1` | Platform | SSRF guard fail-closed | (webhook signing tests) | HMAC-SHA256 fan-out wired | Owner enables per-tenant |
| 29 | OpenClaw Owner Copilot | **CONFIGURED-INERT** | `app/api/owner_copilot.py`, blueprint node `owner_copilot` LOCAL-ONLY | `OPENCLAW_ENABLED=1` | Owner | Owner OS remains sole action authority; Boss/OpenClaw ≠ 32nd agent | (OpenClaw admin tests within Stage A) | LOCAL-ONLY per blueprint | Stage B AMBER approvals design only per `## Current State` |
| 30 | DPDP consent + retention | **PRODUCTION-PROVEN** | `app/platform/dpdp.py`, `app/platform/data_privacy.py`, blueprint node `dpdp` | Consent ledger + 90-day recording retention + purge API | Arnav (Security) | Fail-closed lookup for compliance | (data-retention tests) | Blueprint edge `dpdp -> telephony_vobiz` (`guards`) | Nothing (baseline compliance) |
| 31 | RBAC + tenant isolation | **CODE-PRESENT** | `app/platform/rbac.py`, `app/platform/tenant_manager.py`, blueprint node `rbac` | Roles + module grants | Arnav | Tenant middleware fail-open on billing meters; fail-closed on compliance | (tenant-isolation audit tests) | Blueprint edge `rbac -> app_fastapi` (`guards`) | Full-audit sweep beyond billing pending (per landmines) |
| 32 | Sub-admin team access | **CODE-PRESENT** | `app/api/admin_ops.py`, team access ops runbook | Admin token + module grants | Arnav | Audit-logged | (team access ops tests) | Wired via RBAC | Nothing |

**Row totals by label:** 32 rows · PRODUCTION-PROVEN = 7 · TEST-PROVEN = 1 · CODE-PRESENT = 12 · CONFIGURED-INERT = 8 · HARD-OFF = 3 · OWNER-ACTION-REQUIRED = 1 (row 5 has this as an additional attribute but is labelled CODE-PRESENT until deploy completes).

## Top 5 code-owned repair patches (identified — NOT implemented)

The patches below are each concrete, additive, and would not flip any runtime flag. Per the task contract they are **not implemented** in this session (each is > 20 lines or touches a flag-adjacent surface). Owner authorisation should precede implementation.

### Patch #1 — Add canonical `sales_autopilot` node to the Master Blueprint

- **File:** `app/platform/blueprint_detail_nodes.py` — extend `DETAIL_NODE_SPECS` (`:25-145`).
- **Why:** `app/api/sales_autopilot_admin.py:28,57-164` and `app/platform/scheduler_config.py:199-203,211` prove the subsystem is registered as a scheduler job and as an admin router mounted from `app/main.py:803-807`, yet the canonical blueprint has zero representation. The blueprint validator (`blueprint_graph.py:1334-1546`) will not catch this drift because the node is simply *absent*.
- **Suggested spec:** `depth_level=1`, `parent_domain_id="email_outreach"` (or a new domain `sales_autopilot` if the council prefers), `flags=["SALES_AUTOPILOT_ENABLED","SALES_AUTOPILOT_DRY_RUN","SALES_AUTOPILOT_WHATSAPP_ENABLED","SALES_AUTOPILOT_EMAIL_ENABLED"]`, `safety_lane="AMBER"`, `status="CODE-PRESENT"`, `files=["app/platform/sales_autopilot/send.py","app/api/sales_autopilot_admin.py"]`.
- **Cost:** ~15 lines in `DETAIL_NODE_SPECS`; but it also needs a flow reference (adding `parent_flow_id`) and possibly a new flow — so real footprint is ≥25 lines when done right.

### Patch #2 — Add canonical `creative_os` node to the Master Blueprint

- **File:** `app/platform/blueprint_detail_nodes.py`.
- **Why:** `app/marketing/creative_os/service.py:36,97,240,417,438,539` and `app/marketing/creative_os/brief.py` are ON DISK per `## Current State`, but the canonical graph does not mention them. Anyone reading `/api/blueprint/graph` today sees no Creative OS at all.
- **Suggested spec:** `depth_level=1`, `parent_domain_id="content_gen"`, `flags=["CREATIVE_OS_ENABLED","CREATIVE_PROVIDER_QWEN_IMAGE","CREATIVE_PROVIDER_FLUX_SCHNELL","CREATIVE_PROVIDER_WAN22","CREATIVE_PROVIDER_COMFYUI","CREATIVE_GPU_LAB_ENABLED"]`, `safety_lane="RED"` (per ADR-143 fail-closed), `status="CODE-PRESENT"`, `files=["app/marketing/creative_os/service.py","app/marketing/creative_os/brief.py"]`.
- **Cost:** ~15 lines core + edges to `content_auto` and `social_publish`; ≥25 lines end-to-end.

### Patch #3 — Add canonical `owner_email_canary` node to the Master Blueprint

- **File:** `app/platform/blueprint_detail_nodes.py`.
- **Why:** PR #187 landed `app/api/owner_email_canary.py:17,27,32,52` (`/api/admin/owner-email-canary/*`), `app/platform/owner_email_canary.py`, and a runtime-data allowlist entry. `tests/test_owner_email_canary.py` covers it. The graph does not reflect this new observability control plane.
- **Suggested spec:** `depth_level=1`, `parent_domain_id="observability_ops"`, `safety_lane="GREEN"`, `status="CODE-PRESENT"`, `files=["app/api/owner_email_canary.py","app/platform/owner_email_canary.py"]`, plus one `emits` edge from `owner_email_canary` to `automation_health` and one `observes` edge from `owner_email_canary` to `obs_stack`.
- **Cost:** ~12 lines node + 2 edges ≈ 20 lines. **Closest to eligible** for the "<20 lines tiny + safe" bar, but crosses domain boundaries into edges, so it still needs review.

### Patch #4 — Wire a Sales Autopilot admin tab into `frontend/automation.html`

- **File:** `frontend/automation.html` (tab list around `:60-90`, section list around `:200+`).
- **Why:** The current "Sales Team AI" tab (`:73,217-260`) is the **5-agent prospect deep-dive** (Riya/Veer/Dev/Isha/Arjun) — a fundamentally different feature. `/api/sales-autopilot/{summary,policy,prospects,attempts,eligibility/preview,run-canary,inbound/classify,seed-estique}` is a live admin router with no cockpit; today an operator must call it with curl. The section should show `SALES_AUTOPILOT_ENABLED` state, per-channel kill switches, canary batch size, and the last `run-canary` result — all read-only until owner arms.
- **Cost:** ≥60 lines HTML + JS (tab button, section, fetch handlers, kill switch UX). Cannot be done as a "tiny" patch. Owner sign-off + design review needed.

### Patch #5 — Split a dedicated `/api/automation-flags/*` admin router out of `growth.py`

- **File:** new `app/api/automation_flags_admin.py`; wire mount inside `app/main.py`. `app/api/automation_flags.py` stays a pure registry module.
- **Why:** The only way to read the flag registry live today is `/api/growth/infra/flags` (`app/api/growth.py:1446-1468`). That path buries a critical operator surface inside the growth domain and is hard to alert on. A dedicated router would let admins hit `/api/automation-flags/status`, `/api/automation-flags/gates` (surface each flag's default, kill precedence, and dependent flags), and enable a test asserting every `JOB_META` scheduler job in `RUN_DUE_EXCLUDE` maps to a documented safety-gate flag.
- **Cost:** ~40-60 lines of router + a new test module. Not tiny; also runs into "first-route-wins" — grep of any existing `/api/automation-flags` mount required.

### What must stay HARD-OFF (do not enable in any patch)

- `PLATFORM_DIAL_DAILY` — 3-layer kill; user-mandate 2026-07-05.
- `WHATSAPP_AUTO_SEND` — ban-safety (row 21).
- `REPLY_AUTO_SEND` — deliverability + reputation risk (row 22, `REPLY_AUTO_SEND_HARD_OFF=1` overrides always).
- `UPI_AUTO_ACTIVATE` — billing containment (row 27, 2026-07-18 mandate).
- Cold bulk email — nothing new beyond the existing 25/day cap; do not raise it or bypass warmup.
- Sales Autopilot live channels (`SALES_AUTOPILOT_{WHATSAPP,EMAIL}_ENABLED`, `SALES_AUTOPILOT_DRY_RUN=0`) — canary must remain owner-armed only.

## Provenance & how to falsify this file

- Blueprint counts derived from reading `app/platform/blueprint_graph.py:456-989` and `app/platform/blueprint_detail_nodes.py:25-145` directly; run `python -m app.platform.blueprint_graph` in the container for a re-validation.
- Scheduler contract read from `app/platform/scheduler_config.py:37-211`; `run_due` implementation at `:369-405` confirms the exclusion set.
- Flag registry read from `app/api/automation_flags.py:9-391`; live admin exposure at `app/api/growth.py:1446-1468`.
- PRODUCTION-PROVEN rows use the CLAUDE.md / task-header declaration as source-of-truth and are falsifiable by a `curl.exe https://leadsgenai.in/health` returning `environment != production` or `version != 58a3b70` once the current rollout finishes.
