# CI Health — `prod_check + pytest` failing on main (f8a5f6e9)

**Date:** 2026-07-18 · **Observed via:** GitHub Actions run 29651948817 (job 88099674235) on PR #55.
**Impact:** the `prod_check + pytest` job fails (~15 tests) on `main`/`f8a5f6e9`. It is the **non-required** pytest gate (per CLAUDE.md "full pytest CI me non-blocking"; lint + secrets + GitGuardian + Trivy + the dedicated `test` job all PASS), so PRs stay `MERGEABLE` — but it erodes merge confidence across all 10 open PRs (incl. dependabot #40–48) and hides real regressions.

**Not caused by PR #55.** PR #55 changes only `.gitignore` + 2 docs; none of the failing tests touch those. These failures pre-exist on `f8a5f6e9`.

## Failure clusters (root causes)

### 1. Rate-limit test contamination (429) — 5 failures — HIGH, clearest fix
`tests/test_signup_password_hygiene.py` (`letmein`, `iloveyou`, `  password  `, `test_signup_accepts_reasonable_password`, `test_signup_still_rejects_too_short`) all get `429 Too Many Requests` (`scope:signup`, "Bahut zyada requests") instead of 422/200. The signup rate limiter's counter persists across tests and trips mid-suite.
**Fix:** in the test harness (a `conftest.py` fixture, autouse) reset/disable the signup limiter between tests — e.g. clear its store or set a test env flag that bypasses `scope:signup` limiting under pytest. `tests/conftest.py` is NOT in the parallel WIP → safe to edit in isolation.

### 2. Reduced route set in the test app — 6 failures — HIGH, single likely root cause
`test_studio_media::test_media_routes_mounted` (missing `/api/customer/studio/upload`), `test_track_features::test_booking_router_mounted` (missing `/api/booking/slots`), `test_upi_invoice_and_studio::test_studio_new_routes_mounted_and_authed` (missing `/api/customer/studio/upi-qr`), `test_vobiz` ×3 (401 instead of 503/200 — auth fires before route logic). The mounted-route set is small (`{'', '/api/status', '/app/admin', …}`), implying **many routers silently failed to mount in the test app** — the CLAUDE.md landmine class (function-level import failure → router not registered → exception handler masks the real ImportError). Likely ONE broken import cascades.
**Fix:** reproduce the test-app build, find the router(s) not mounting (grep `include_router` in `app/main.py`, force-import each in a test), surface the masked ImportError. Touches app import wiring — coordinate with parallel WIP owners.

### 3. Stale assertions vs current behaviour — 2 failures — MEDIUM
- `test_voice_agent::…contains_professionalism_rule` asserts `'sir/madam'` in the system prompt; the prompt was reworded (Hinglish, no literal translations) and no longer contains it. → update the test to assert the current professionalism marker (product-intent check needed — is "sir/madam" required, or is the new wording correct?).
- `test_track_upgrades::test_whatsapp_reply_helper_writes_draft` expects intent `'question'`, got `'other'` — classifier drift. → confirm expected classification, update test or classifier.

### 4. Log-capture assertions empty — 2 failures — MEDIUM
`test_social_dry_run_visibility::test_dry_run_drain_logs_a_loud_warning` (asserts 'dry-run' in captured log, got '') and `test_startup_secret_redaction::test_startup_banner_never_logs_google_maps_key` (asserts banner text in captured log, got ''). Log capture isn't seeing output — likely a logger-propagation/`caplog` config issue in CI, or the log level/handler changed. → fix the test's log capture (set `caplog.set_level`, ensure logger propagates) or the code's logging path.

## PR #55 status
`MERGEABLE`, draft, mergeStateStatus `UNSTABLE` (only the non-required pytest job red). Safe for the **owner** to review + mark ready + merge — it introduces no runtime code. Recommend the owner merges #55 independently of this CI-health work.

## Recommended fix sequence (each = isolated PR off origin/main, clean worktree)
1. **Cluster 1 (rate-limit isolation)** — smallest, highest-confidence; `tests/conftest.py` autouse reset. `fix/test-signup-ratelimit-isolation`.
2. **Cluster 2 (router mount)** — highest product value (real "route missing" = the function-level-import landmine); find + fix the masked ImportError. `fix/test-app-router-mount`.
3. **Clusters 3 & 4 (stale assertions + log capture)** — after confirming product intent. `fix/pytest-assertion-drift`.

Do not bundle these; do not edit files currently in the parallel WIP set without owner coordination. Run `scripts/check_secrets.py` + the targeted suite before each PR.

## Verification evidence
- `gh pr checks 55`: `prod_check + pytest` = fail; `Lint + syntax + secrets` = pass; `GitGuardian` = pass; `Trivy repo + SBOM` = pass; `test` = pass.
- Failing assertions captured from run 29651948817 job 88099674235 (`Process completed with exit code 1`).
- Prod unaffected: `https://leadsgenai.in/health` = healthy, f8a5f6e9 (these are TEST failures, not runtime failures — the running app serves the affected routes in prod; the test app under-mounts them).
