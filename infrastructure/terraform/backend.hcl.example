# Terraform Backend Configuration
# Copy this file to backend.hcl and update with your project values
# Then run: terraform init -backend-config=backend.hcl

bucket = "YOUR_PROJECT_ID-terraform-state"
